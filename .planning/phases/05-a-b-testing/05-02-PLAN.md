---
phase: 05-a-b-testing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/campaigns/VariantTabStrip.tsx
  - src/components/campaigns/SplitPercentageInput.tsx
  - src/pages/campaigns/AbTestBuilderPage.tsx
autonomous: true
requirements:
  - ABTS-01
  - ABTS-02

must_haves:
  truths:
    - "User can see a tab strip with Variant A and Variant B tabs on the A/B test builder"
    - "User can enter different subject lines for each variant"
    - "User can compose different email bodies with TipTap editor for each variant"
    - "User can set a test group size slider/input and see derived split breakdown"
    - "User can save an A/B test as draft with shared settings + two variant subjects/bodies"
    - "User can send both test variants to their respective contact subsets"
  artifacts:
    - path: "src/components/campaigns/VariantTabStrip.tsx"
      provides: "Tab strip component for Variant A / Variant B switching"
      exports: ["VariantTabStrip"]
    - path: "src/components/campaigns/SplitPercentageInput.tsx"
      provides: "Test group size input with derived breakdown display"
      exports: ["SplitPercentageInput"]
    - path: "src/pages/campaigns/AbTestBuilderPage.tsx"
      provides: "Full A/B test builder page with shared settings, tab strip, dual TipTap editors"
      exports: ["AbTestBuilderPage"]
  key_links:
    - from: "src/pages/campaigns/AbTestBuilderPage.tsx"
      to: "src/hooks/campaigns/useCampaigns.ts"
      via: "createAbTest and sendAbTestVariants functions"
      pattern: "createAbTest|sendAbTestVariants"
    - from: "src/pages/campaigns/AbTestBuilderPage.tsx"
      to: "src/hooks/campaigns/useAbTest.ts"
      via: "useAbTest hook for edit mode population"
      pattern: "useAbTest"
    - from: "src/pages/campaigns/AbTestBuilderPage.tsx"
      to: "src/components/campaigns/VariantTabStrip.tsx"
      via: "VariantTabStrip component import"
      pattern: "VariantTabStrip"
---

<objective>
Build the A/B test builder page with shared settings, variant tab strip, split percentage input, and dual TipTap editors.

Purpose: Users can create and edit A/B test campaigns with two variants (different subject lines and/or email bodies) and set the split percentage between them (ABTS-01, ABTS-02).
Output: Three new component files — VariantTabStrip, SplitPercentageInput, and the full AbTestBuilderPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-a-b-testing/05-CONTEXT.md
@.planning/phases/05-a-b-testing/05-RESEARCH.md
@.planning/phases/05-a-b-testing/05-UI-SPEC.md
@.planning/phases/05-a-b-testing/05-01-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 01 -->

From src/types/database.ts (after Plan 01):
```typescript
export type CampaignType = 'regular' | 'ab_test' | 'ab_variant'

export interface Campaign {
  id: string
  workspace_id: string
  name: string
  status: CampaignStatus
  from_name: string
  from_email: string
  reply_to_email: string | null
  subject: string
  preview_text: string | null
  body_html: string
  body_json: Record<string, unknown> | null
  contact_list_id: string | null
  settings: Record<string, unknown>
  campaign_type: CampaignType
  parent_campaign_id: string | null
  // ... stat fields, timestamps
}

export interface AbTestSettings {
  split_percentage: number
  hold_back_contact_ids?: string[]
}
```

From src/hooks/campaigns/useAbTest.ts (Plan 01):
```typescript
export function useAbTest(parentId: string | undefined): {
  parent: Campaign | null
  variantA: Campaign | null
  variantB: Campaign | null
  loading: boolean
  error: string | null
  refetch: () => Promise<void>
}
```

From src/hooks/campaigns/useCampaigns.ts (after Plan 01):
```typescript
createAbTest: (payload: {
  name: string
  fromName: string
  fromEmail: string
  replyTo: string | null
  contactListId: string
  splitPercentage: number
  variantA: { subject: string; bodyHtml: string; bodyJson: Record<string, unknown> | null }
  variantB: { subject: string; bodyHtml: string; bodyJson: Record<string, unknown> | null }
}) => Promise<{ data: { parent: Campaign; variantA: Campaign; variantB: Campaign } | null; error: string | null }>

sendAbTestVariants: (
  parentId: string,
  variantAId: string,
  variantBId: string,
  splitPercentage: number,
  contactListId: string
) => Promise<{ error: string | null }>

updateCampaign: (id: string, updates: CampaignUpdate) => Promise<{ error: string | null }>
```

From src/pages/campaigns/CampaignBuilderPage.tsx (TipTap editor config — DO NOT modify this file):
```typescript
import { useEditor, EditorContent } from '@tiptap/react'
import StarterKit from '@tiptap/starter-kit'
import Link from '@tiptap/extension-link'
import Image from '@tiptap/extension-image'
import Placeholder from '@tiptap/extension-placeholder'
import { VariableChipNode } from '../../components/campaigns/VariableChipNode'
import { VariableSlashCommand } from '../../components/campaigns/VariableSlashCommand'
import { CampaignEditorToolbar } from '../../components/campaigns/CampaignEditorToolbar'
```

From src/hooks/contacts/useContactLists.ts:
```typescript
export function useContactLists(): { lists: ContactList[]; ... }
```

From src/components/ui (reusable components):
```typescript
// Button: variant='primary'|'secondary'|'ghost'|'danger', size='sm'|'md'|'lg', loading, disabled
// Card: padding='sm'|'md'|'lg', className
// Input: label, value, onChange, error, placeholder
// Spinner: size='sm'|'md'|'lg'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VariantTabStrip + SplitPercentageInput components</name>
  <files>src/components/campaigns/VariantTabStrip.tsx, src/components/campaigns/SplitPercentageInput.tsx</files>
  <read_first>
    - src/pages/campaigns/CampaignBuilderPage.tsx (tab pattern reference from edit/preview toggle — lines 481-511)
    - src/components/ui/Input.tsx (Input component interface for styling reference)
    - .planning/phases/05-a-b-testing/05-UI-SPEC.md (Interaction States section for exact Tailwind classes)
    - .planning/phases/05-a-b-testing/05-CONTEXT.md (D-03 tab strip, D-04 split model)
  </read_first>
  <action>
**1. Create `src/components/campaigns/VariantTabStrip.tsx`:**

Per D-03 and UI-SPEC Interaction States:

```typescript
interface VariantTabStripProps {
  activeTab: 'A' | 'B'
  onTabChange: (tab: 'A' | 'B') => void
}

export function VariantTabStrip({ activeTab, onTabChange }: VariantTabStripProps) {
  return (
    <div className="border-b border-gray-800 flex gap-6 mb-6" role="tablist">
      {(['A', 'B'] as const).map((tab) => (
        <button
          key={tab}
          role="tab"
          aria-selected={activeTab === tab}
          type="button"
          onClick={() => onTabChange(tab)}
          className={`pb-3 text-sm font-medium border-b-2 ${
            activeTab === tab
              ? 'text-gray-100 border-indigo-500'
              : 'text-gray-400 border-transparent hover:text-gray-200'
          }`}
        >
          Variant {tab}
        </button>
      ))}
    </div>
  )
}
```

Key details:
- `role="tablist"` + `role="tab"` + `aria-selected` for accessibility
- Active state: `text-gray-100 border-indigo-500` (matches UI-SPEC exactly)
- Inactive state: `text-gray-400 border-transparent hover:text-gray-200`
- Container: `border-b border-gray-800 flex gap-6 mb-6`

**2. Create `src/components/campaigns/SplitPercentageInput.tsx`:**

Per D-04 and UI-SPEC:

```typescript
interface SplitPercentageInputProps {
  value: number
  onChange: (value: number) => void
}

export function SplitPercentageInput({ value, onChange }: SplitPercentageInputProps) {
  const halfPct = value / 2
  const holdBackPct = 100 - value
  const isValid = value >= 8 && value <= 90

  return (
    <div className="flex flex-col gap-2">
      <label className="text-sm font-medium text-gray-300">Test group size</label>
      <div className="flex items-center gap-3">
        <input
          type="number"
          min={8}
          max={90}
          step={2}
          value={value}
          onChange={(e) => {
            const num = parseInt(e.target.value, 10)
            if (!isNaN(num)) onChange(num)
          }}
          className={`w-24 bg-gray-800 border text-gray-100 text-sm rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-indigo-500 ${
            isValid ? 'border-gray-700' : 'border-red-500'
          }`}
        />
        <span className="text-sm text-gray-400">%</span>
      </div>
      {isValid ? (
        <p className="text-sm text-gray-400">
          Variant A: {halfPct}% &middot; Variant B: {halfPct}% &middot; Hold-back: {holdBackPct}%
        </p>
      ) : (
        <p className="text-sm text-red-400">
          Test group must be between 8% and 90%
        </p>
      )}
    </div>
  )
}
```

Key details:
- Number input with `min={8}` `max={90}` `step={2}` — step of 2 ensures even split
- Derived breakdown text: "Variant A: X% . Variant B: X% . Hold-back: Y%" (per D-04 and UI-SPEC Copywriting Contract)
- Invalid state shows red error text
- Default value of 40 is set by the parent component (AbTestBuilderPage), not here
- Input styling matches existing `Input` component pattern: `bg-gray-800 border border-gray-700 text-gray-100 text-sm rounded-lg px-3 py-2`
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/campaigns/VariantTabStrip.tsx exists and contains `export function VariantTabStrip`
    - src/components/campaigns/VariantTabStrip.tsx contains `role="tablist"`
    - src/components/campaigns/VariantTabStrip.tsx contains `aria-selected={activeTab === tab}`
    - src/components/campaigns/VariantTabStrip.tsx contains `text-gray-100 border-indigo-500`
    - src/components/campaigns/VariantTabStrip.tsx contains `text-gray-400 border-transparent`
    - src/components/campaigns/SplitPercentageInput.tsx exists and contains `export function SplitPercentageInput`
    - src/components/campaigns/SplitPercentageInput.tsx contains `min={8}`
    - src/components/campaigns/SplitPercentageInput.tsx contains `max={90}`
    - src/components/campaigns/SplitPercentageInput.tsx contains `Hold-back:`
    - src/components/campaigns/SplitPercentageInput.tsx contains `Test group must be between 8% and 90%`
    - npm run build exits 0
  </acceptance_criteria>
  <done>VariantTabStrip renders accessible tabs with correct active/inactive styles. SplitPercentageInput shows test group size with derived Variant A / Variant B / Hold-back breakdown and validation. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: AbTestBuilderPage with dual TipTap editors and full form</name>
  <files>src/pages/campaigns/AbTestBuilderPage.tsx</files>
  <read_first>
    - src/pages/campaigns/CampaignBuilderPage.tsx (TipTap editor config lines 58-69, populated guard pattern lines 72-97, form state pattern, save/send handler patterns — read FULL file as reference but DO NOT modify it)
    - src/components/campaigns/VariableChipNode.tsx (TipTap extension import path)
    - src/components/campaigns/VariableSlashCommand.tsx (TipTap extension import path)
    - src/components/campaigns/CampaignEditorToolbar.tsx (toolbar component interface — expects `editor` prop)
    - src/components/campaigns/VariableDropdown.tsx (VARIABLES constant export for subject variable insertion)
    - src/hooks/campaigns/useAbTest.ts (Plan 01 output — useAbTest hook interface)
    - src/hooks/campaigns/useCampaigns.ts (Plan 01 output — createAbTest, sendAbTestVariants, updateCampaign)
    - src/hooks/contacts/useContactLists.ts (useContactLists hook for target list selector)
    - src/components/campaigns/VariantTabStrip.tsx (Task 1 output)
    - src/components/campaigns/SplitPercentageInput.tsx (Task 1 output)
  </read_first>
  <action>
Replace the placeholder `AbTestBuilderPage.tsx` created in Plan 01 Task 3 with the full implementation.

**The AbTestBuilderPage follows the same patterns as CampaignBuilderPage but with key differences:**
- Two TipTap editor instances (one per variant) — both always mounted, display toggled via CSS `block`/`hidden` (per RESEARCH.md Pattern 5)
- Separate subject state per variant
- Shared settings (name, from_name, from_email, contact_list_id, split_percentage) at top
- VariantTabStrip between shared settings and editor panel
- Separate `dirtyA` / `dirtyB` + `populatedA` / `populatedB` flags (avoids Pitfall 4 from RESEARCH.md)

**Imports required:**
```typescript
import { useState, useEffect, useCallback, useRef } from 'react'
import { useParams, useNavigate } from 'react-router-dom'
import { useEditor, EditorContent } from '@tiptap/react'
import StarterKit from '@tiptap/starter-kit'
import Link from '@tiptap/extension-link'
import Image from '@tiptap/extension-image'
import Placeholder from '@tiptap/extension-placeholder'
import { VariableChipNode } from '../../components/campaigns/VariableChipNode'
import { VariableSlashCommand } from '../../components/campaigns/VariableSlashCommand'
import { CampaignEditorToolbar } from '../../components/campaigns/CampaignEditorToolbar'
import { VARIABLES } from '../../components/campaigns/VariableDropdown'
import { VariantTabStrip } from '../../components/campaigns/VariantTabStrip'
import { SplitPercentageInput } from '../../components/campaigns/SplitPercentageInput'
import { Input } from '../../components/ui/Input'
import { Button } from '../../components/ui/Button'
import { Spinner } from '../../components/ui/Spinner'
import { Card } from '../../components/ui/Card'
import { useCampaigns } from '../../hooks/campaigns/useCampaigns'
import { useAbTest } from '../../hooks/campaigns/useAbTest'
import { useContactLists } from '../../hooks/contacts/useContactLists'
import { useToast } from '../../components/ui/Toast'
import type { AbTestSettings } from '../../types/database'
```

**Component state:**
```typescript
export function AbTestBuilderPage() {
  const { id } = useParams<{ id: string }>()
  const navigate = useNavigate()
  const { showToast } = useToast()
  const isNew = !id

  // Data hooks
  const { createAbTest, sendAbTestVariants, updateCampaign } = useCampaigns()
  const { parent, variantA: existingA, variantB: existingB, loading: abLoading } = useAbTest(id)
  const { lists } = useContactLists()

  // Shared settings state
  const [name, setName] = useState('')
  const [fromName, setFromName] = useState('')
  const [fromEmail, setFromEmail] = useState('')
  const [replyTo, setReplyTo] = useState('')
  const [contactListId, setContactListId] = useState<string | null>(null)
  const [splitPercentage, setSplitPercentage] = useState(40)  // D-04 default

  // Per-variant state
  const [subjectA, setSubjectA] = useState('')
  const [subjectB, setSubjectB] = useState('')
  const [activeTab, setActiveTab] = useState<'A' | 'B'>('A')
  const [dirtyA, setDirtyA] = useState(false)
  const [dirtyB, setDirtyB] = useState(false)
  const [populatedA, setPopulatedA] = useState(false)
  const [populatedB, setPopulatedB] = useState(false)

  // UI state
  const [errors, setErrors] = useState<Record<string, string>>({})
  const [saving, setSaving] = useState(false)
  const [sending, setSending] = useState(false)

  // Subject variable insertion refs (per variant)
  const subjectARef = useRef<HTMLInputElement>(null)
  const subjectBRef = useRef<HTMLInputElement>(null)
  const [subjectVarOpen, setSubjectVarOpen] = useState(false)
  const subjectVarDropdownRef = useRef<HTMLDivElement>(null)
```

**Two TipTap editor instances** (both created at mount, never destroyed):
```typescript
  const editorA = useEditor({
    extensions: [
      StarterKit,
      Link.configure({ openOnClick: false }),
      Image,
      Placeholder.configure({ placeholder: 'Write Variant A email content here...' }),
      VariableChipNode,
      VariableSlashCommand,
    ],
    content: '',
    onUpdate: () => { if (populatedA) setDirtyA(true) },
  })

  const editorB = useEditor({
    extensions: [
      StarterKit,
      Link.configure({ openOnClick: false }),
      Image,
      Placeholder.configure({ placeholder: 'Write Variant B email content here...' }),
      VariableChipNode,
      VariableSlashCommand,
    ],
    content: '',
    onUpdate: () => { if (populatedB) setDirtyB(true) },
  })
```

The `onUpdate` callbacks check `populatedA`/`populatedB` flags to avoid false dirty state during `setContent` population (Pitfall 4).

**Edit mode population** (from useAbTest data):
```typescript
  useEffect(() => {
    if (parent && !populatedA && !populatedB && editorA && editorB) {
      // Shared settings from parent
      setName(parent.name ?? '')
      setFromName(parent.from_name ?? '')
      setFromEmail(parent.from_email ?? '')
      setReplyTo(parent.reply_to_email ?? '')
      setContactListId(parent.contact_list_id ?? null)
      const settings = parent.settings as AbTestSettings | null
      setSplitPercentage(settings?.split_percentage ?? 40)

      // Variant A
      if (existingA) {
        setSubjectA(existingA.subject ?? '')
        if (existingA.body_json) {
          editorA.commands.setContent(existingA.body_json)
        } else if (existingA.body_html) {
          editorA.commands.setContent(existingA.body_html)
        }
      }
      setPopulatedA(true)

      // Variant B
      if (existingB) {
        setSubjectB(existingB.subject ?? '')
        if (existingB.body_json) {
          editorB.commands.setContent(existingB.body_json)
        } else if (existingB.body_html) {
          editorB.commands.setContent(existingB.body_html)
        }
      }
      setPopulatedB(true)

      setDirtyA(false)
      setDirtyB(false)
    }
  }, [parent, existingA, existingB, editorA, editorB, populatedA, populatedB])
```

**Subject variable insertion handler** (reuse pattern from CampaignBuilderPage):
```typescript
  const handleSubjectVariableInsert = useCallback((variableValue: string) => {
    const input = activeTab === 'A' ? subjectARef.current : subjectBRef.current
    const currentSubject = activeTab === 'A' ? subjectA : subjectB
    const setter = activeTab === 'A' ? setSubjectA : setSubjectB
    if (!input) return
    const start = input.selectionStart ?? currentSubject.length
    const end = input.selectionEnd ?? currentSubject.length
    const token = `{{${variableValue}}}`
    const newValue = currentSubject.slice(0, start) + token + currentSubject.slice(end)
    setter(newValue)
    if (activeTab === 'A') setDirtyA(true)
    else setDirtyB(true)
    requestAnimationFrame(() => {
      input.focus()
      const pos = start + token.length
      input.setSelectionRange(pos, pos)
    })
  }, [activeTab, subjectA, subjectB])
```

**Click-outside for subject variable dropdown:**
```typescript
  useEffect(() => {
    function handleClickOutside(e: MouseEvent) {
      if (subjectVarDropdownRef.current && !subjectVarDropdownRef.current.contains(e.target as Node)) {
        setSubjectVarOpen(false)
      }
    }
    if (subjectVarOpen) document.addEventListener('mousedown', handleClickOutside)
    return () => document.removeEventListener('mousedown', handleClickOutside)
  }, [subjectVarOpen])
```

**Validation:**
```typescript
  const validate = () => {
    const newErrors: Record<string, string> = {}
    if (!name.trim()) newErrors.name = 'Test name is required.'
    if (!fromName.trim()) newErrors.fromName = 'Sender name is required.'
    if (!fromEmail.trim()) newErrors.fromEmail = 'Sender email is required.'
    if (!contactListId) newErrors.contactListId = 'Please select a target list.'
    if (!subjectA.trim()) newErrors.subjectA = 'Variant A subject is required.'
    if (!subjectB.trim()) newErrors.subjectB = 'Variant B subject is required.'
    const bodyA = editorA?.getHTML() ?? ''
    const bodyB = editorB?.getHTML() ?? ''
    if (!bodyA || bodyA === '<p></p>') newErrors.bodyA = 'Variant A body cannot be empty.'
    if (!bodyB || bodyB === '<p></p>') newErrors.bodyB = 'Variant B body cannot be empty.'
    if (splitPercentage < 8 || splitPercentage > 90) newErrors.split = 'Test group must be between 8% and 90%.'
    setErrors(newErrors)
    return Object.keys(newErrors).length === 0
  }
```

**Save draft handler:**
```typescript
  const handleSaveDraft = async () => {
    setSaving(true)
    if (isNew) {
      // Create mode — use createAbTest
      const { data, error } = await createAbTest({
        name: name || 'Untitled A/B test',
        fromName,
        fromEmail,
        replyTo: replyTo || null,
        contactListId: contactListId!,
        splitPercentage,
        variantA: {
          subject: subjectA,
          bodyHtml: editorA?.getHTML() ?? '',
          bodyJson: editorA?.getJSON() ?? null,
        },
        variantB: {
          subject: subjectB,
          bodyHtml: editorB?.getHTML() ?? '',
          bodyJson: editorB?.getJSON() ?? null,
        },
      })
      if (error) {
        showToast(error, 'error')
      } else if (data) {
        showToast('A/B test draft saved.', 'success')
        setDirtyA(false)
        setDirtyB(false)
        navigate(`/campaigns/${data.parent.id}/ab-test/edit`, { replace: true })
      }
    } else {
      // Edit mode — update parent + both variants
      const parentUpdate = await updateCampaign(id!, {
        name: name || 'Untitled A/B test',
        from_name: fromName,
        from_email: fromEmail,
        reply_to_email: replyTo || null,
        contact_list_id: contactListId,
        settings: { split_percentage: splitPercentage } as unknown as Record<string, unknown>,
      })
      if (parentUpdate.error) { showToast(parentUpdate.error, 'error'); setSaving(false); return }

      if (existingA) {
        const vaUpdate = await updateCampaign(existingA.id, {
          subject: subjectA,
          body_html: editorA?.getHTML() ?? '',
          body_json: editorA?.getJSON() ?? null,
          from_name: fromName,
          from_email: fromEmail,
          reply_to_email: replyTo || null,
          contact_list_id: contactListId,
        })
        if (vaUpdate.error) { showToast(vaUpdate.error, 'error'); setSaving(false); return }
      }

      if (existingB) {
        const vbUpdate = await updateCampaign(existingB.id, {
          subject: subjectB,
          body_html: editorB?.getHTML() ?? '',
          body_json: editorB?.getJSON() ?? null,
          from_name: fromName,
          from_email: fromEmail,
          reply_to_email: replyTo || null,
          contact_list_id: contactListId,
        })
        if (vbUpdate.error) { showToast(vbUpdate.error, 'error'); setSaving(false); return }
      }

      showToast('A/B test draft saved.', 'success')
      setDirtyA(false)
      setDirtyB(false)
    }
    setSaving(false)
  }
```

**Send test variants handler:**
```typescript
  const handleSendVariants = async () => {
    if (!validate()) return
    if (!contactListId) return

    // Save first (create if new, update if existing)
    setSending(true)

    let parentId = id
    let vaId = existingA?.id
    let vbId = existingB?.id

    if (isNew) {
      const { data, error } = await createAbTest({
        name: name || 'Untitled A/B test',
        fromName,
        fromEmail,
        replyTo: replyTo || null,
        contactListId,
        splitPercentage,
        variantA: {
          subject: subjectA,
          bodyHtml: editorA?.getHTML() ?? '',
          bodyJson: editorA?.getJSON() ?? null,
        },
        variantB: {
          subject: subjectB,
          bodyHtml: editorB?.getHTML() ?? '',
          bodyJson: editorB?.getJSON() ?? null,
        },
      })
      if (error || !data) {
        showToast(error || 'Failed to create A/B test', 'error')
        setSending(false)
        return
      }
      parentId = data.parent.id
      vaId = data.variantA.id
      vbId = data.variantB.id
    } else {
      // Save latest changes first
      await handleSaveDraft()
      if (!existingA || !existingB) {
        showToast('Variants not loaded. Please refresh.', 'error')
        setSending(false)
        return
      }
    }

    // Now send both variants
    const { error: sendError } = await sendAbTestVariants(
      parentId!,
      vaId!,
      vbId!,
      splitPercentage,
      contactListId
    )

    if (sendError) {
      showToast(sendError, 'error')
    } else {
      showToast('A/B test variants are sending.', 'success')
      navigate('/campaigns')
    }
    setSending(false)
  }
```

**Render structure** (matches UI-SPEC Layout Patterns):

```tsx
  // Loading state
  if (!isNew && abLoading) {
    return (
      <div className="flex items-center justify-center h-64">
        <Spinner size="lg" />
      </div>
    )
  }

  const canSend = !parent || parent.status === 'draft'

  return (
    <div className="flex flex-col gap-8 max-w-4xl">
      {/* Page header */}
      <div className="flex items-start justify-between gap-4 border-b border-gray-800 pb-4">
        <input
          type="text"
          value={name}
          onChange={(e) => { setName(e.target.value); setDirtyA(true) }}
          placeholder="A/B test name..."
          className="text-xl font-semibold text-gray-100 bg-transparent border-0 border-b border-gray-700 focus:border-indigo-500 focus:outline-none flex-1 py-1"
        />
        <div className="flex gap-2 shrink-0">
          <Button variant="secondary" size="sm" loading={saving} onClick={handleSaveDraft}>
            Save draft
          </Button>
          <Button
            variant="primary"
            size="md"
            loading={sending}
            disabled={!canSend || sending}
            onClick={handleSendVariants}
          >
            {sending ? 'Sending...' : 'Send test variants'}
          </Button>
        </div>
      </div>
      {errors.name && <p className="text-sm text-red-400 -mt-6" role="alert">{errors.name}</p>}

      {/* Shared Settings */}
      <Card>
        <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-4">Shared Settings</p>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          <Input label="From name" value={fromName} onChange={(e) => { setFromName(e.target.value); setDirtyA(true) }} placeholder="e.g. Alex from MailOps" error={errors.fromName} />
          <Input label="From email" type="email" value={fromEmail} onChange={(e) => { setFromEmail(e.target.value); setDirtyA(true) }} placeholder="noreply@yourdomain.com" error={errors.fromEmail} />
          <Input label="Reply-to (optional)" type="email" value={replyTo} onChange={(e) => setReplyTo(e.target.value)} placeholder="replies@yourdomain.com" />
          <div className="flex flex-col gap-1">
            <label className="text-sm font-medium text-gray-300">Target list</label>
            <select
              value={contactListId ?? ''}
              onChange={(e) => { setContactListId(e.target.value || null); setDirtyA(true) }}
              className={`bg-gray-800 border text-gray-100 text-sm rounded-lg px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-indigo-500 ${errors.contactListId ? 'border-red-500' : 'border-gray-700'}`}
            >
              <option value="" disabled>Select a contact list...</option>
              {lists.map((list) => (
                <option key={list.id} value={list.id}>{list.name} ({list.contact_count} contacts)</option>
              ))}
            </select>
            {errors.contactListId && <p className="text-sm text-red-400" role="alert">{errors.contactListId}</p>}
          </div>
        </div>
        <div className="mt-4">
          <SplitPercentageInput value={splitPercentage} onChange={setSplitPercentage} />
        </div>
      </Card>

      {/* Variant Tab Strip */}
      <VariantTabStrip activeTab={activeTab} onTabChange={setActiveTab} />

      {/* Variant Editor Panels — BOTH always mounted, toggled via CSS */}
      <div className={activeTab === 'A' ? 'block' : 'hidden'}>
        {/* Variant A subject */}
        <div className="flex flex-col gap-1 mb-4">
          <label className="text-sm font-medium text-gray-300">Subject line</label>
          <div className="flex gap-2 items-start">
            <input
              ref={subjectARef}
              type="text"
              value={subjectA}
              onChange={(e) => { setSubjectA(e.target.value); setDirtyA(true) }}
              placeholder="Variant A subject..."
              className={`flex-1 bg-gray-800 border text-gray-100 text-sm rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-indigo-500 ${errors.subjectA ? 'border-red-500' : 'border-gray-700'}`}
            />
            <div className="relative" ref={activeTab === 'A' ? subjectVarDropdownRef : undefined}>
              <button type="button" onClick={() => setSubjectVarOpen(!subjectVarOpen)} className="px-2 py-2 bg-gray-800 border border-gray-700 rounded-lg text-gray-400 hover:text-gray-200 hover:bg-gray-700 text-sm" title="Insert variable">{'{{ }}'}</button>
              {subjectVarOpen && activeTab === 'A' && (
                <div className="absolute top-full right-0 mt-1 bg-gray-800 border border-gray-700 rounded-lg shadow-lg p-2 z-20 min-w-[180px]">
                  {VARIABLES.map((v) => (
                    <button key={v.value} type="button" className="flex items-center gap-2 px-3 py-2 text-sm text-gray-200 hover:bg-gray-700 rounded-md cursor-pointer w-full text-left" onClick={() => { handleSubjectVariableInsert(v.value); setSubjectVarOpen(false) }}>
                      {v.label}
                    </button>
                  ))}
                </div>
              )}
            </div>
          </div>
          {errors.subjectA && <p className="text-sm text-red-400" role="alert">{errors.subjectA}</p>}
        </div>
        {/* Variant A editor */}
        {editorA && (
          <div className="border border-gray-700 rounded-lg overflow-hidden">
            <CampaignEditorToolbar editor={editorA} />
            <EditorContent editor={editorA} className="min-h-[320px] p-4 text-gray-100 text-sm leading-relaxed bg-gray-900 focus:outline-none prose-invert" />
          </div>
        )}
        {errors.bodyA && <p className="text-sm text-red-400 mt-2" role="alert">{errors.bodyA}</p>}
      </div>

      <div className={activeTab === 'B' ? 'block' : 'hidden'}>
        {/* Variant B subject */}
        <div className="flex flex-col gap-1 mb-4">
          <label className="text-sm font-medium text-gray-300">Subject line</label>
          <div className="flex gap-2 items-start">
            <input
              ref={subjectBRef}
              type="text"
              value={subjectB}
              onChange={(e) => { setSubjectB(e.target.value); setDirtyB(true) }}
              placeholder="Variant B subject..."
              className={`flex-1 bg-gray-800 border text-gray-100 text-sm rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-indigo-500 ${errors.subjectB ? 'border-red-500' : 'border-gray-700'}`}
            />
            <div className="relative" ref={activeTab === 'B' ? subjectVarDropdownRef : undefined}>
              <button type="button" onClick={() => setSubjectVarOpen(!subjectVarOpen)} className="px-2 py-2 bg-gray-800 border border-gray-700 rounded-lg text-gray-400 hover:text-gray-200 hover:bg-gray-700 text-sm" title="Insert variable">{'{{ }}'}</button>
              {subjectVarOpen && activeTab === 'B' && (
                <div className="absolute top-full right-0 mt-1 bg-gray-800 border border-gray-700 rounded-lg shadow-lg p-2 z-20 min-w-[180px]">
                  {VARIABLES.map((v) => (
                    <button key={v.value} type="button" className="flex items-center gap-2 px-3 py-2 text-sm text-gray-200 hover:bg-gray-700 rounded-md cursor-pointer w-full text-left" onClick={() => { handleSubjectVariableInsert(v.value); setSubjectVarOpen(false) }}>
                      {v.label}
                    </button>
                  ))}
                </div>
              )}
            </div>
          </div>
          {errors.subjectB && <p className="text-sm text-red-400" role="alert">{errors.subjectB}</p>}
        </div>
        {/* Variant B editor */}
        {editorB && (
          <div className="border border-gray-700 rounded-lg overflow-hidden">
            <CampaignEditorToolbar editor={editorB} />
            <EditorContent editor={editorB} className="min-h-[320px] p-4 text-gray-100 text-sm leading-relaxed bg-gray-900 focus:outline-none prose-invert" />
          </div>
        )}
        {errors.bodyB && <p className="text-sm text-red-400 mt-2" role="alert">{errors.bodyB}</p>}
      </div>
    </div>
  )
}
```

Note the critical detail: both editor `<div>`s use `block`/`hidden` CSS classes (not conditional rendering) to preserve TipTap state and undo history across tab switches.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `export function AbTestBuilderPage`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `useEditor` (appears twice — one for editorA, one for editorB)
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `editorA` and `editorB`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `VariantTabStrip`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `SplitPercentageInput`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `createAbTest`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `sendAbTestVariants`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `useAbTest`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `populatedA` and `populatedB`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `activeTab === 'A' ? 'block' : 'hidden'`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `Send test variants`
    - src/pages/campaigns/AbTestBuilderPage.tsx contains `Save draft`
    - npm run build exits 0
  </acceptance_criteria>
  <done>AbTestBuilderPage fully implements D-02 (separate flow), D-03 (tab strip with dual TipTap editors), D-04 (split percentage input). Users can create/edit A/B tests with shared settings + independent variant subjects and bodies. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form input -> Supabase insert | All inserts include workspace_id from profile; RLS enforces workspace isolation |
| Client -> Edge Function | sendAbTestVariants invokes send-campaign with JWT; workspace checked in function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | Split percentage input | mitigate | Client-side validation: min=8, max=90 in SplitPercentageInput; validate() checks range before send |
| T-05-08 | Tampering | Subject/body XSS | accept | TipTap sanitizes HTML output; Supabase RLS prevents cross-workspace reads; email clients sandbox HTML further |
| T-05-09 | Information Disclosure | Form pre-population in edit mode | mitigate | useAbTest query scopes to workspace_id; only own variants are loaded |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` passes
2. VariantTabStrip renders accessible tab strip with active/inactive states
3. SplitPercentageInput shows derived breakdown text and validates 8-90 range
4. AbTestBuilderPage has dual TipTap editors with CSS toggle (not conditional render)
5. Save draft creates 3 campaign rows (parent + 2 variants) or updates existing
6. Send test variants calls sendAbTestVariants with Fisher-Yates split
</verification>

<success_criteria>
- VariantTabStrip component renders Variant A / Variant B tabs with correct active/inactive styling
- SplitPercentageInput shows "Variant A: X% . Variant B: X% . Hold-back: Y%" derived from input value
- AbTestBuilderPage loads in create mode (/campaigns/ab-test/new) and edit mode (/campaigns/:id/ab-test/edit)
- Two TipTap editors maintain independent state across tab switches (CSS block/hidden toggle)
- Save draft persists parent campaign + both variants to database
- Send test variants triggers Edge Function invocations for both variants with contact_ids subsets
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-a-b-testing/05-02-SUMMARY.md`
</output>
