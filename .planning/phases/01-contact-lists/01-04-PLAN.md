---
phase: 01-contact-lists
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/components/contacts/ContactsTable.tsx
  - src/components/contacts/ContactsFilters.tsx
  - src/components/contacts/ContactDrawer.tsx
autonomous: true
requirements: [CONT-05, CONT-06, CONT-07, CONT-08, CONT-09, LIST-03, LIST-04]
must_haves:
  truths:
    - "User can see contacts in a paginated table with name, email, status, tags, and date columns"
    - "User can search contacts by email or name with 300ms debounce"
    - "User can filter contacts by status, tag, and custom field key-value"
    - "User can click a contact row to open a slide-in drawer"
    - "User can edit contact fields inline in the drawer and save changes"
    - "User can delete a contact via the drawer danger zone with inline confirmation"
    - "User can add/remove a contact from lists within the drawer"
    - "Drawer uses position:fixed and does not get clipped by AppLayout overflow-hidden"
  artifacts:
    - path: "src/components/contacts/ContactsTable.tsx"
      provides: "Paginated contacts table with row click handler"
      contains: "Page"
    - path: "src/components/contacts/ContactsFilters.tsx"
      provides: "Search bar + filter controls for contacts"
      contains: "Search contacts"
    - path: "src/components/contacts/ContactDrawer.tsx"
      provides: "Slide-in detail drawer with edit/delete/list-membership"
      contains: "fixed right-0 top-0"
  key_links:
    - from: "src/components/contacts/ContactsTable.tsx"
      to: "src/components/contacts/ContactDrawer.tsx"
      via: "onContactClick callback opens drawer with selected contact"
      pattern: "setSelectedContact"
    - from: "src/components/contacts/ContactsFilters.tsx"
      to: "src/hooks/contacts/useContacts.ts"
      via: "Filter state passed as ContactFilters to useContacts hook"
      pattern: "ContactFilters"
---

<objective>
Build the All Contacts tab: the contacts table with pagination, the filter bar, and the contact detail drawer with full CRUD and list membership management.

Purpose: Delivers the core contact management experience -- viewing, searching, filtering, editing, deleting contacts, and managing list membership (CONT-05 through CONT-09, LIST-03, LIST-04).
Output: Three components that plug into ContactsPage from Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contact-lists/01-CONTEXT.md
@.planning/phases/01-contact-lists/01-RESEARCH.md
@.planning/phases/01-contact-lists/01-UI-SPEC.md

<interfaces>
From src/types/database.ts:
```typescript
export interface Contact {
  id: string
  workspace_id: string
  email: string
  first_name: string | null
  last_name: string | null
  company: string | null
  tags: string[]
  custom_fields: Record<string, string>
  status: 'active' | 'unsubscribed' | 'bounced' | 'complained'
  unsubscribed_at: string | null
  bounce_type: 'hard' | 'soft' | null
  bounced_at: string | null
  created_at: string
  updated_at: string
  deleted_at: string | null
}

export interface ContactList {
  id: string; workspace_id: string; name: string; description: string | null
  color: string | null; contact_count: number; ...
}

export interface ContactListMember {
  id: string; contact_list_id: string; contact_id: string; added_at: string
}

export type ContactUpdate = Partial<Omit<Contact, 'id' | 'workspace_id' | 'created_at' | 'updated_at' | 'deleted_at'>>
```

From src/hooks/contacts/useContacts.ts:
```typescript
export interface ContactFilters {
  search?: string; status?: string; tag?: string
  customFieldKey?: string; customFieldValue?: string
  listId?: string; page?: number; pageSize?: number
}
export function useContacts(filters: ContactFilters): {
  contacts: Contact[]; loading: boolean; error: string | null
  refetch: () => Promise<void>; totalCount: number
}
```

From src/hooks/contacts/useContactLists.ts:
```typescript
export function useContactLists(): {
  lists: ContactList[]; loading: boolean; error: string | null
  refetch: () => Promise<void>; createList: ...; updateList: ...; deleteList: ...
}
```

From src/components/ui/:
```typescript
// Button: variant (primary/secondary/ghost/danger), size (sm/md/lg), loading
// Input: label, error, icon, all HTMLInput attrs
// Card: padding (sm/md/lg)
// Badge: variant (default/success/warning/danger/info)
// Spinner: size (sm/md/lg)
```

From src/lib/supabase.ts:
```typescript
export const supabase: SupabaseClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactsFilters and ContactsTable components</name>
  <files>src/components/contacts/ContactsFilters.tsx, src/components/contacts/ContactsTable.tsx</files>
  <read_first>
    - .planning/phases/01-contact-lists/01-UI-SPEC.md (Contacts Table and Filter Bar sections for exact styling)
    - .planning/phases/01-contact-lists/01-CONTEXT.md (D-02, CONT-06 through CONT-09)
    - src/components/ui/Input.tsx (Input API for search bar)
    - src/components/ui/Button.tsx (Button API for pagination/clear)
    - src/components/ui/Badge.tsx (Badge API for status display)
    - src/components/ui/Card.tsx (Card API for table wrapper)
    - src/components/ui/Spinner.tsx (Spinner API for loading state)
    - src/hooks/contacts/useContacts.ts (ContactFilters interface)
  </read_first>
  <action>
Create `src/components/contacts/ContactsFilters.tsx`:

Props: `{ filters: ContactFilters; onFiltersChange: (filters: ContactFilters) => void }`

Layout: `flex flex-wrap gap-3 items-center mb-4`

Components inside the filter bar:
1. Search `Input` with `Search` Lucide icon, placeholder "Search contacts...", `className="w-64"`. Uses local state + 300ms debounce via useEffect cleanup. On change: update local value immediately, debounce the `onFiltersChange` call.
2. Status `<select>` styled with `bg-gray-800 border border-gray-700 text-gray-100 text-sm rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-indigo-500`. Options: "All statuses" (value ""), "Active", "Unsubscribed", "Bounced", "Complained". On change: call `onFiltersChange` with updated status.
3. Tag `Input`, placeholder "Filter by tag...", `className="w-40"`. Same debounce pattern as search.
4. Custom field: two `Input` fields side by side — "Field name" (w-32) + "Field value" (w-32). Both debounced.
5. "Clear filters" `Button variant="ghost" size="sm"` — only visible when any filter is active (search || status || tag || customFieldKey). Clicking resets all filters to defaults.

Debounce implementation: `useState` for each text input, `useEffect` with 300ms `setTimeout` that calls `onFiltersChange`, cleanup returns `clearTimeout`.

Create `src/components/contacts/ContactsTable.tsx`:

Props: `{ contacts: Contact[]; loading: boolean; totalCount: number; page: number; pageSize: number; onPageChange: (page: number) => void; onContactClick: (contact: Contact) => void }`

Structure:
- Outer: `Card padding="sm"`
- Loading state: `Spinner size="md"` centered inside card (`flex items-center justify-center py-12`)
- Empty state (contacts.length === 0 and not loading): centered text -- heading "No contacts yet" (`text-lg font-semibold text-gray-300`) + body "Import a CSV file or add contacts manually to get started." (`text-sm text-gray-500 mt-1`)
- Empty search state (contacts.length === 0, not loading, and filters are active — pass a `hasFilters` prop): heading "No contacts found" + body "Try adjusting your search or clearing the filters."

Table structure:
- `<table className="w-full">`
- Header row: `text-xs font-semibold text-gray-400 uppercase tracking-wide bg-gray-900 border-b border-gray-800`
- Columns: Name (first + last combined) | Email | Status | Tags | Added | (no explicit actions column — row click opens drawer)
- Body rows: `h-12 border-b border-gray-800/50 hover:bg-gray-800/50 cursor-pointer transition-colors`
- Row click handler: `onClick={() => onContactClick(contact)}` on the `<tr>`
- Name cell: `text-sm text-gray-100 font-medium` showing `${contact.first_name || ''} ${contact.last_name || ''}`.trim() or `--` if both null
- Email cell: `text-sm text-gray-300`
- Status cell: `<Badge variant={statusVariant}>` where active=success, unsubscribed=warning, bounced=danger, complained=warning. Display the status text inside the badge.
- Tags cell: render up to 3 tags as `<Badge variant="default">` pills. If more than 3, show `+N more` in `text-xs text-gray-500`. Each tag badge: `text-xs`
- Added cell: `text-sm text-gray-500` showing relative date. Use a simple helper function: if < 1 day -> "Today", < 2 days -> "Yesterday", < 7 days -> "N days ago", < 30 days -> "N weeks ago", else format as "MMM D, YYYY" using `Date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`

Pagination below table:
- `flex items-center justify-between mt-4`
- Left: "Page {page} of {totalPages}" (`text-sm text-gray-400`)
- Right: Previous `Button variant="secondary" size="sm"` (disabled if page === 1) + Next `Button variant="secondary" size="sm"` (disabled if page >= totalPages)
- `totalPages = Math.ceil(totalCount / pageSize)` — show pagination only if totalPages > 1
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/contacts/ContactsFilters.tsx contains "Search contacts..."
    - src/components/contacts/ContactsFilters.tsx contains "All statuses"
    - src/components/contacts/ContactsFilters.tsx contains "Filter by tag..."
    - src/components/contacts/ContactsFilters.tsx contains "Field name"
    - src/components/contacts/ContactsFilters.tsx contains "Field value"
    - src/components/contacts/ContactsFilters.tsx contains "Clear filters"
    - src/components/contacts/ContactsFilters.tsx contains "setTimeout" (debounce)
    - src/components/contacts/ContactsTable.tsx contains "No contacts yet"
    - src/components/contacts/ContactsTable.tsx contains "No contacts found"
    - src/components/contacts/ContactsTable.tsx contains "Page"
    - src/components/contacts/ContactsTable.tsx contains "hover:bg-gray-800/50"
    - src/components/contacts/ContactsTable.tsx contains "onContactClick"
    - src/components/contacts/ContactsTable.tsx contains "Badge"
    - TypeScript compiles
  </acceptance_criteria>
  <done>ContactsFilters renders search + status/tag/custom-field filters with 300ms debounce. ContactsTable renders a paginated table with status badges, tag pills, and row click handler.</done>
</task>

<task type="auto">
  <name>Task 2: Create ContactDrawer with edit/delete/list-membership and wire all into ContactsPage</name>
  <files>src/components/contacts/ContactDrawer.tsx, src/pages/contacts/ContactsPage.tsx</files>
  <read_first>
    - .planning/phases/01-contact-lists/01-UI-SPEC.md (Contact Drawer section for exact styling, states, sections)
    - .planning/phases/01-contact-lists/01-CONTEXT.md (D-06 through D-09)
    - src/components/layout/AppLayout.tsx (verify overflow-hidden — drawer MUST use position:fixed)
    - src/pages/contacts/ContactsPage.tsx (Plan 03 output — must integrate table/filters/drawer into this page)
    - src/lib/supabase.ts (Supabase client for direct CRUD operations)
    - src/hooks/contacts/useContacts.ts (ContactFilters, useContacts)
    - src/hooks/contacts/useContactLists.ts (useContactLists for list membership)
  </read_first>
  <action>
Create `src/components/contacts/ContactDrawer.tsx`:

Props:
```typescript
interface ContactDrawerProps {
  contact: Contact | null
  onClose: () => void
  onUpdated: () => void
  onDeleted: () => void
}
```

CRITICAL: Use `position: fixed` (Tailwind `fixed`) for both backdrop and drawer panel. AppLayout has `overflow-hidden` which clips `absolute` positioned children.

Structure (per D-06, D-07, D-08):
- Backdrop: `fixed inset-0 bg-black/40 z-40` — onClick calls `onClose`
- Drawer panel: `fixed right-0 top-0 h-full w-96 bg-gray-900 border-l border-gray-800 z-50 overflow-y-auto shadow-xl transition-transform duration-200`
  - When open: `translate-x-0`; when closed/null contact: `translate-x-full`
  - If `contact` is null, return null (don't render)
- Header: `flex items-center justify-between p-4 border-b border-gray-800`
  - Left: contact name or "Contact" (`text-base font-semibold text-gray-100`)
  - Right: X button `Button variant="ghost" size="sm"` with `X` Lucide icon, `aria-label="Close"`

Body: `p-4 flex flex-col gap-6 overflow-y-auto`

**Section 1: Contact Fields**
Section heading: `text-xs font-semibold text-gray-400 uppercase tracking-wide mb-2` — "Contact Details"
Default state: read-only display of fields
- Email: label `text-xs text-gray-500` + value `text-sm text-gray-100`
- First Name, Last Name, Company: same pattern
- Status: `Badge` with appropriate variant
- Tags: row of `Badge variant="default"` pills

"Edit" `Button variant="secondary" size="sm"` toggles to edit mode:
- Fields become `Input` components with current values
- "Save changes" `Button variant="primary" size="sm" loading={saving}` + "Discard changes" `Button variant="ghost" size="sm"` replace "Edit"
- Save calls: `supabase.from('contacts').update({ first_name, last_name, company, tags, email }).eq('id', contact.id).eq('workspace_id', profile.workspace_id)` then shows toast "Contact saved." and calls `onUpdated()`
- Tags editing: `Input` with comma-separated values, parsed into string array on save

**Section 2: List Membership** (per D-09, LIST-03)
Section heading: "Lists"
- Show current list memberships: for each list the contact belongs to, show list name with color dot + "Remove" `Button variant="ghost" size="sm"` with `X` icon
- Fetch memberships: `supabase.from('contact_list_members').select('*, contact_lists(id, name, color)').eq('contact_id', contact.id)`
- "Add to list" dropdown: show available lists (from useContactLists) that the contact is NOT already in. Select triggers: `supabase.from('contact_list_members').insert({ contact_list_id: listId, contact_id: contact.id })`. Show toast "Contact added to list."
- Remove from list: `supabase.from('contact_list_members').delete().eq('contact_list_id', listId).eq('contact_id', contact.id)`. Show toast "Contact removed from list."

**Section 3: Danger Zone**
Section heading: "Danger Zone"
- "Delete Contact" `Button variant="danger" size="sm"` — clicking replaces with inline confirmation:
  - Warning text: "Are you sure you want to delete {first_name}? This cannot be undone." (`text-sm text-gray-300`)
  - "Yes, delete contact" `Button variant="danger" size="sm"` + "Keep contact" `Button variant="ghost" size="sm"`
- Delete action: `supabase.from('contacts').update({ deleted_at: new Date().toISOString() }).eq('id', contact.id).eq('workspace_id', profile.workspace_id)`. Toast "Contact deleted." then call `onDeleted()`.

**Also create "Add Contact" functionality:**
When ContactsPage "Add Contact" button is clicked, open ContactDrawer with a new empty contact (contact = null triggers "new contact" mode). In new mode:
- Header shows "New Contact"
- Fields start as editable Inputs (no read-only state)
- Email is required (show validation error if empty)
- Save calls: `supabase.from('contacts').insert({ email, first_name, last_name, company, tags, workspace_id: profile.workspace_id, status: 'active', custom_fields: {} })`. Toast "Contact added." then call `onUpdated()`
- No Danger Zone section in new mode

Update `src/pages/contacts/ContactsPage.tsx` to wire everything together:
- Import and use `useContacts(filters)` and `useContactLists()` hooks
- Manage state: `filters` (ContactFilters), `selectedContact` (Contact | null), `showNewContact` (boolean), `page` (number)
- Render `ContactsFilters` + `ContactsTable` in All Contacts tab area (replace the placeholder div)
- Render `ContactDrawer` for selected contact or new contact mode
- When `?list=<id>` is set: pass `listId` to useContacts filters to show only list members
- Wire "Add Contact" button to open drawer in new mode
- Wire contact row click to open drawer with that contact
- On drawer close/update/delete: refetch contacts
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/contacts/ContactDrawer.tsx contains "fixed right-0 top-0"
    - src/components/contacts/ContactDrawer.tsx contains "fixed inset-0 bg-black/40 z-40"
    - src/components/contacts/ContactDrawer.tsx contains "w-96 bg-gray-900 border-l border-gray-800 z-50"
    - src/components/contacts/ContactDrawer.tsx contains "Delete Contact"
    - src/components/contacts/ContactDrawer.tsx contains "Yes, delete contact"
    - src/components/contacts/ContactDrawer.tsx contains "Keep contact"
    - src/components/contacts/ContactDrawer.tsx contains "Save changes"
    - src/components/contacts/ContactDrawer.tsx contains "Discard changes"
    - src/components/contacts/ContactDrawer.tsx contains "Contact added."  or "Contact saved." (toast messages)
    - src/components/contacts/ContactDrawer.tsx contains "contact_list_members" (list membership queries)
    - src/components/contacts/ContactDrawer.tsx contains "deleted_at: new Date().toISOString()" (soft delete)
    - src/components/contacts/ContactDrawer.tsx contains "aria-label=\"Close\""
    - src/pages/contacts/ContactsPage.tsx contains "useContacts"
    - src/pages/contacts/ContactsPage.tsx contains "ContactsFilters"
    - src/pages/contacts/ContactsPage.tsx contains "ContactsTable"
    - src/pages/contacts/ContactsPage.tsx contains "ContactDrawer"
    - TypeScript compiles
  </acceptance_criteria>
  <done>ContactDrawer opens from right with edit/delete/list-membership. ContactsPage is fully wired with filters, table, and drawer. All contact CRUD operations work through the drawer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> contact fields | Email, name, company, tags entered via drawer form, stored in DB |
| User input -> search/filter | Search text interpolated into PostgREST .or() filter string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ContactDrawer edit | mitigate | All Supabase update/delete queries include .eq('workspace_id', profile.workspace_id); RLS backup |
| T-04-02 | Tampering | ContactsFilters search | mitigate | Search input trimmed before interpolation into .or() PostgREST filter; RLS prevents cross-workspace access regardless |
| T-04-03 | Information Disclosure | ContactDrawer list membership | mitigate | contact_list_members queries scoped through contact_lists.workspace_id via RLS join policy |
| T-04-04 | Elevation of Privilege | Contact soft delete | mitigate | Soft delete uses .update({ deleted_at }) with .eq('workspace_id') — not a hard DELETE; RLS enforces ownership |
| T-04-05 | Denial of Service | ContactsFilters debounce | mitigate | 300ms debounce on all text inputs prevents rapid-fire Supabase queries |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Contacts table renders with pagination
- Filter bar debounces and filters update the table
- Drawer opens on row click with contact details
- Edit/save/delete operations work in drawer
- List membership add/remove works in drawer
</verification>

<success_criteria>
- User can view contacts in paginated table (CONT-05)
- User can search by email/name (CONT-06)
- User can filter by tag (CONT-07), status (CONT-08), custom field (CONT-09)
- User can click a contact to open drawer (D-06)
- User can edit, delete, manage list membership in drawer (D-07, D-09, CONT-05, LIST-03)
- Drawer stays visible with table behind it (D-08)
- User can view contacts within a specific list via ?list=<id> (LIST-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-contact-lists/01-04-SUMMARY.md`
</output>
