---
phase: 01-contact-lists
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/pages/contacts/ContactsPage.tsx
  - src/components/contacts/ListsGrid.tsx
  - src/components/contacts/CreateListModal.tsx
  - src/components/contacts/ColorPicker.tsx
  - src/App.tsx
autonomous: true
requirements: [LIST-01, LIST-02, LIST-05]
must_haves:
  truths:
    - "User can navigate to /contacts and see two tabs: All Contacts and Lists"
    - "User can click the Lists tab and see list cards with names, descriptions, counts, and color bars"
    - "User can create a new list with name, description, and color via a modal"
    - "User can rename and delete lists from the card options menu"
    - "Contact lists display accurate contact_count values"
    - "Clicking a list card navigates to /contacts?list=<id>"
  artifacts:
    - path: "src/pages/contacts/ContactsPage.tsx"
      provides: "Main contacts page with tab switching and URL param state"
      contains: "useSearchParams"
    - path: "src/components/contacts/ListsGrid.tsx"
      provides: "Grid layout of list cards with actions menu"
      contains: "grid-cols-1 sm:grid-cols-2 xl:grid-cols-3"
    - path: "src/components/contacts/CreateListModal.tsx"
      provides: "Modal for creating a new contact list"
      contains: "Create List"
    - path: "src/components/contacts/ColorPicker.tsx"
      provides: "8-color preset chip selector"
      contains: "#6366f1"
    - path: "src/App.tsx"
      provides: "Updated route pointing to ContactsPage"
      contains: "ContactsPage"
  key_links:
    - from: "src/pages/contacts/ContactsPage.tsx"
      to: "src/hooks/contacts/useContactLists.ts"
      via: "useContactLists() hook call"
      pattern: "useContactLists"
    - from: "src/App.tsx"
      to: "src/pages/contacts/ContactsPage.tsx"
      via: "Route element import"
      pattern: "import.*ContactsPage"
---

<objective>
Build the ContactsPage shell with tab navigation and the complete Lists tab (ListsGrid, CreateListModal, ColorPicker). Wire the /contacts route.

Purpose: Establishes the page structure (per D-01) and delivers LIST-01 (create list), LIST-02 (rename/delete), LIST-05 (contact count display). The All Contacts tab content area is rendered as a placeholder in this plan and filled by Plan 04.
Output: Working /contacts page with functional Lists tab; All Contacts tab shows a loading/empty placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contact-lists/01-CONTEXT.md
@.planning/phases/01-contact-lists/01-RESEARCH.md
@.planning/phases/01-contact-lists/01-UI-SPEC.md

<interfaces>
<!-- From Plan 02 outputs — hooks and types the executor uses -->

From src/types/database.ts:
```typescript
export interface ContactList {
  id: string
  workspace_id: string
  name: string
  description: string | null
  color: string | null
  contact_count: number
  created_at: string
  updated_at: string
  deleted_at: string | null
}
```

From src/hooks/contacts/useContactLists.ts:
```typescript
export function useContactLists(): {
  lists: ContactList[]
  loading: boolean
  error: string | null
  refetch: () => Promise<void>
  createList: (name: string, description: string | null, color: string | null) => Promise<{ error: string | null }>
  updateList: (id: string, updates: { name?: string; description?: string | null; color?: string | null }) => Promise<{ error: string | null }>
  deleteList: (id: string) => Promise<{ error: string | null }>
}
```

From src/components/ui/Button.tsx:
```typescript
interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger'
  size?: 'sm' | 'md' | 'lg'
  loading?: boolean
}
export function Button(props: ButtonProps): JSX.Element
```

From src/components/ui/Input.tsx:
```typescript
// Input with label, icon, error support
export function Input(props: InputProps): JSX.Element
```

From src/components/ui/Card.tsx:
```typescript
// Card with padding variants sm/md/lg
export function Card(props: CardProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactsPage with tab navigation and update App.tsx route</name>
  <files>src/pages/contacts/ContactsPage.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (current route definitions — must replace PlaceholderPage for /contacts)
    - src/pages/dashboard/DashboardPage.tsx (reference for page component pattern)
    - src/components/layout/AppLayout.tsx (layout structure the page renders inside)
    - .planning/phases/01-contact-lists/01-UI-SPEC.md (Tab Navigation section for exact styling)
    - .planning/phases/01-contact-lists/01-CONTEXT.md (D-01 through D-05 decisions)
  </read_first>
  <action>
Create `src/pages/contacts/ContactsPage.tsx`:

This is the main page component for `/contacts`. It manages:
- Tab state: "All Contacts" vs "Lists" (per D-01)
- URL query param `?list=<id>` for deep-linking (per D-05)
- Renders ListsGrid (from this plan) or a placeholder div for the contacts table (filled by Plan 04)

Implementation details:
- Import `useSearchParams` from `react-router-dom`
- `const activeListId = searchParams.get('list')` — if present, Lists tab is active with that list selected
- Two tab buttons rendered below the page header
- Active tab styling: `text-indigo-400 border-b-2 border-indigo-500`
- Inactive tab styling: `text-gray-400 hover:text-gray-200`
- Page header contains: "Contacts" title (`text-xl font-semibold text-gray-100`), "Import Contacts" Button (primary), "Add Contact" Button (secondary), "View import history" Button (ghost, sm)
- For now, "Import Contacts", "Add Contact", and "View import history" buttons are rendered but their click handlers are empty functions (Plan 04 and 05 will wire them)
- When "All Contacts" tab is active and no `?list=` param: render a div with id="contacts-table-slot" containing text "Loading contacts..." (Plan 04 replaces this with ContactsTable)
- When "Lists" tab is active: render `<ListsGrid />` component
- When `?list=<id>` is set: render the breadcrumb strip (colored dot + list name + "All Contacts" clear link) above a placeholder for the filtered contacts table
- Breadcrumb strip styling per UI-SPEC: `flex items-center gap-2 mb-4 text-sm`; list name `text-gray-100 font-semibold`; separator `text-gray-600`; "All Contacts" link `text-indigo-400 hover:text-indigo-300 cursor-pointer`

Tab switching logic:
- Clicking "All Contacts" tab: `setSearchParams({})` (clears list param)
- Clicking "Lists" tab: only change internal tab state, do NOT set URL param
- The tab state derives from: if `activeListId` exists -> show contacts table filtered; otherwise use local `activeTab` state

Update `src/App.tsx`:
- Replace `import { PlaceholderPage } from './pages/PlaceholderPage'` usage for contacts route only
- Add: `import { ContactsPage } from './pages/contacts/ContactsPage'`
- Change line `<Route path="/contacts" element={<PlaceholderPage title="Contacts" />} />` to `<Route path="/contacts" element={<ContactsPage />} />`
- Keep PlaceholderPage import since campaigns, templates, analytics still use it
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/contacts/ContactsPage.tsx contains "useSearchParams"
    - src/pages/contacts/ContactsPage.tsx contains "All Contacts" (tab label)
    - src/pages/contacts/ContactsPage.tsx contains "Lists" (tab label)
    - src/pages/contacts/ContactsPage.tsx contains "text-indigo-400 border-b-2 border-indigo-500" (active tab)
    - src/pages/contacts/ContactsPage.tsx contains "Import Contacts" (CTA button text)
    - src/pages/contacts/ContactsPage.tsx contains "searchParams.get('list')"
    - src/App.tsx contains "import { ContactsPage }" (or "import ContactsPage")
    - src/App.tsx contains 'element={<ContactsPage />}'
    - src/App.tsx does NOT contain 'PlaceholderPage title="Contacts"'
    - TypeScript compiles
  </acceptance_criteria>
  <done>ContactsPage renders with two tabs, URL param support for list deep-linking, and the /contacts route points to it.</done>
</task>

<task type="auto">
  <name>Task 2: Create ListsGrid, CreateListModal, and ColorPicker components</name>
  <files>src/components/contacts/ListsGrid.tsx, src/components/contacts/CreateListModal.tsx, src/components/contacts/ColorPicker.tsx</files>
  <read_first>
    - .planning/phases/01-contact-lists/01-UI-SPEC.md (Lists Grid, Create List Modal, ColorPicker sections for exact styling)
    - .planning/phases/01-contact-lists/01-CONTEXT.md (D-03, LIST-01, LIST-02, LIST-05)
    - src/components/ui/Button.tsx (Button API — variant, size, loading props)
    - src/components/ui/Card.tsx (Card API — padding prop)
    - src/components/ui/Input.tsx (Input API — label, error props)
    - src/hooks/contacts/useContactLists.ts (hook API created in Plan 02)
  </read_first>
  <action>
Create `src/components/contacts/ColorPicker.tsx`:

A controlled component that renders 8 color chips in a flex row.

Props: `{ value: string | null; onChange: (color: string) => void }`

Color palette (exactly these hex values per UI-SPEC):
```typescript
const COLORS = [
  { name: 'Indigo', value: '#6366f1' },
  { name: 'Green', value: '#22c55e' },
  { name: 'Amber', value: '#f59e0b' },
  { name: 'Red', value: '#ef4444' },
  { name: 'Cyan', value: '#06b6d4' },
  { name: 'Pink', value: '#ec4899' },
  { name: 'Violet', value: '#8b5cf6' },
  { name: 'Slate', value: '#64748b' },
]
```

Each chip: `w-7 h-7 rounded-full border-2 cursor-pointer transition-colors`
Selected: `border-white`; Unselected: `border-transparent`
Each chip has `aria-label={name}`, `role="radio"`, `aria-checked={selected}`
Inline style `backgroundColor` set to the hex value.

Create `src/components/contacts/CreateListModal.tsx`:

Props: `{ open: boolean; onClose: () => void; onCreated: () => void }`

Modal overlay: `fixed inset-0 bg-black/60 z-50 flex items-center justify-center`
Modal panel: `bg-gray-900 border border-gray-800 rounded-xl shadow-2xl w-full max-w-md mx-4`
Header: `flex items-center justify-between p-4 border-b border-gray-800` — title "New List" left, X button right
Body: `p-4 flex flex-col gap-4`
Fields:
  - "List name" `Input` (required) — validate non-empty, show "List name is required." error
  - "Description" `Input` (optional)
  - "Color" `ColorPicker` (default to first color '#6366f1')
Footer: `flex justify-end gap-3 p-4 border-t border-gray-800`
  - "Cancel" `Button variant="ghost"`
  - "Create List" `Button variant="primary" loading={creating}`

On submit: call `useContactLists().createList(name, description, color)`. Show toast on success ("List created.") or error. Close modal and call `onCreated()`.

Import `useToast` from the toast hook for feedback. Import `useContactLists` from the hook.

Create `src/components/contacts/ListsGrid.tsx`:

Props: `{ lists: ContactList[]; loading: boolean; onListClick: (listId: string) => void; onRefresh: () => void }`

Grid layout: `grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4`

Each list card: `Card padding="sm"` with a 4px left color bar. Implementation: wrap Card content in a div with `border-l-4` using inline style `borderLeftColor: list.color || '#64748b'` and `rounded-l-lg`.

Card content layout:
- Top row: list name (`text-sm font-semibold text-gray-100`) + "..." options menu button (`Button variant="ghost" size="sm"`, `aria-label="List options"`) using `MoreHorizontal` Lucide icon
- Description below name: `text-xs text-gray-400 mt-0.5` (only if description exists)
- Contact count: `text-xl font-semibold text-gray-100 mt-3` showing `list.contact_count`
- Count label: `text-xs text-gray-500` showing "contacts"
- Options menu (toggle state): dropdown div `absolute right-0 mt-1 bg-gray-800 border border-gray-700 rounded-lg shadow-lg py-1 z-10 min-w-[140px]`
  - "Rename" option: `px-3 py-2 text-sm text-gray-200 hover:bg-gray-700 cursor-pointer`
  - "Delete list" option: `px-3 py-2 text-sm text-red-400 hover:bg-gray-700 cursor-pointer`
- Rename inline: replace name text with Input, save on Enter/blur
- Delete: show confirmation — text "Delete '{list.name}'? This will remove the list but not the contacts in it." with "Yes, delete list" `Button variant="danger" size="sm"` and Cancel `Button variant="ghost" size="sm"`

"New List" card at the end: dashed border card `border-2 border-dashed border-gray-700 rounded-lg p-4 flex items-center justify-center gap-2 text-gray-400 hover:border-indigo-500 hover:text-indigo-400 cursor-pointer transition-colors`. Uses `Plus` Lucide icon + "New List" text. Clicking opens CreateListModal.

Empty state (when lists.length === 0 and not loading): centered in grid area — "No lists yet" heading (`text-lg font-semibold text-gray-300`) + "Create a list to organize your contacts for targeting campaigns." body (`text-sm text-gray-500 mt-1`)

Loading state: show `Spinner size="md"` centered.

Clicking a list card body (not the menu button) calls `onListClick(list.id)`.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/contacts/ColorPicker.tsx contains "const COLORS ="
    - src/components/contacts/ColorPicker.tsx contains "#6366f1"
    - src/components/contacts/ColorPicker.tsx contains "aria-label="
    - src/components/contacts/ColorPicker.tsx contains "role=\"radio\""
    - src/components/contacts/CreateListModal.tsx contains "Create List"
    - src/components/contacts/CreateListModal.tsx contains "List name is required."
    - src/components/contacts/CreateListModal.tsx contains "bg-gray-900 border border-gray-800 rounded-xl"
    - src/components/contacts/ListsGrid.tsx contains "grid-cols-1 sm:grid-cols-2 xl:grid-cols-3"
    - src/components/contacts/ListsGrid.tsx contains "No lists yet"
    - src/components/contacts/ListsGrid.tsx contains "New List"
    - src/components/contacts/ListsGrid.tsx contains "Delete list"
    - src/components/contacts/ListsGrid.tsx contains "Rename"
    - src/components/contacts/ListsGrid.tsx contains "border-l-4"
    - TypeScript compiles
  </acceptance_criteria>
  <done>ListsGrid renders list cards with color bars, counts, and options menus. CreateListModal handles list creation with validation. ColorPicker provides 8 preset color chips. All compile and are wired to ContactsPage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> list name/description | Text input stored in DB via Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | CreateListModal | mitigate | Validate list name is non-empty before insert; Supabase RLS enforces workspace_id ownership |
| T-03-02 | Elevation of Privilege | ListsGrid delete | mitigate | deleteList includes .eq('workspace_id', profile.workspace_id) in the update query ensuring users can only delete their own lists |
| T-03-03 | Spoofing | List color value | accept | Color is a hex string from a fixed palette; no user-supplied free-text color input; stored as-is in DB |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- /contacts route renders ContactsPage
- Lists tab shows grid of list cards or empty state
- CreateListModal opens, validates, and creates lists
- List cards show color bar, name, description, count
</verification>

<success_criteria>
- User can navigate to /contacts and see two tabs (D-01)
- Lists tab shows list cards with color bars, names, counts (D-03, LIST-05)
- User can create a list with name, description, color (LIST-01)
- User can rename and delete lists (LIST-02)
- Clicking a list card navigates to ?list=<id> (D-04, D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/01-contact-lists/01-03-SUMMARY.md`
</output>
