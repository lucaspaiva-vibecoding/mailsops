---
phase: 01-contact-lists
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/database.ts
  - src/hooks/contacts/useContacts.ts
  - src/hooks/contacts/useContactLists.ts
autonomous: true
requirements: [CONT-05, CONT-06, CONT-07, CONT-08, CONT-09, LIST-01, LIST-02, LIST-03, LIST-04, LIST-05]
must_haves:
  truths:
    - "Contact, ContactList, ContactListMember, and ContactImportLog TypeScript interfaces exist and match schema-v1.md column types"
    - "useContacts hook fetches contacts with workspace_id filter, soft-delete filter, and supports search/status/tag/customField/listId parameters"
    - "useContactLists hook fetches contact lists with workspace_id and soft-delete filters"
  artifacts:
    - path: "src/types/database.ts"
      provides: "Contact, ContactList, ContactListMember, ContactImportLog interfaces + insert/update types"
      contains: "export interface Contact"
    - path: "src/hooks/contacts/useContacts.ts"
      provides: "useContacts hook with filter support, refetch, loading, error states"
      exports: ["useContacts"]
    - path: "src/hooks/contacts/useContactLists.ts"
      provides: "useContactLists hook with CRUD operations"
      exports: ["useContactLists"]
  key_links:
    - from: "src/hooks/contacts/useContacts.ts"
      to: "supabase.from('contacts')"
      via: "Supabase query with .eq('workspace_id') and .is('deleted_at', null)"
      pattern: "eq\\('workspace_id'"
    - from: "src/hooks/contacts/useContactLists.ts"
      to: "supabase.from('contact_lists')"
      via: "Supabase query with .eq('workspace_id') and .is('deleted_at', null)"
      pattern: "eq\\('workspace_id'"
---

<objective>
Define all TypeScript interfaces for contacts and build the data-access hooks that every UI component in the phase will consume.

Purpose: Establishes the type contracts and data layer before any UI components are built. All downstream plans import these types and hooks directly.
Output: Extended database.ts with 4 new interfaces, two custom hooks for contacts and lists data access.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contact-lists/01-CONTEXT.md
@.planning/phases/01-contact-lists/01-RESEARCH.md
@docs/schema-v1.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/database.ts:
```typescript
export interface Profile {
  id: string
  workspace_id: string
  full_name: string | null
  avatar_url: string | null
  company_name: string | null
  timezone: string
  created_at: string
  updated_at: string
}

export interface Database {
  public: {
    Tables: {
      profiles: {
        Row: Profile
        Insert: Partial<Profile> & { id: string }
        Update: Partial<Profile>
      }
    }
  }
}
```

From src/contexts/AuthContext.tsx:
```typescript
interface AuthContextValue {
  user: User | null
  profile: Profile | null
  session: Session | null
  loading: boolean
  signIn: (email: string, password: string) => Promise<{ error: string | null }>
  signUp: (email: string, password: string, fullName: string) => Promise<{ error: string | null }>
  signOut: () => Promise<void>
  resetPassword: (email: string) => Promise<{ error: string | null }>
  refreshProfile: () => Promise<void>
}
```

From src/hooks/useAuth.ts:
```typescript
export function useAuth(): AuthContextValue
// Access: const { profile } = useAuth()
// Key value: profile.workspace_id — used in ALL Supabase queries
```

From src/lib/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js'
export const supabase = createClient(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend database.ts with contact type interfaces</name>
  <files>src/types/database.ts</files>
  <read_first>
    - src/types/database.ts (current file — must preserve existing Profile and Database interfaces)
    - docs/schema-v1.md (source of truth for column names, types, constraints)
    - .planning/phases/01-contact-lists/01-RESEARCH.md (TypeScript Types Needed section has exact interface definitions)
  </read_first>
  <action>
Add these interfaces and types AFTER the existing `Profile` interface and BEFORE the existing `Database` interface in `src/types/database.ts`. Then update the `Database` interface to include the new table types.

Add these exact interfaces:

```typescript
export interface Contact {
  id: string
  workspace_id: string
  email: string
  first_name: string | null
  last_name: string | null
  company: string | null
  tags: string[]
  custom_fields: Record<string, string>
  status: 'active' | 'unsubscribed' | 'bounced' | 'complained'
  unsubscribed_at: string | null
  bounce_type: 'hard' | 'soft' | null
  bounced_at: string | null
  created_at: string
  updated_at: string
  deleted_at: string | null
}

export interface ContactList {
  id: string
  workspace_id: string
  name: string
  description: string | null
  color: string | null
  contact_count: number
  created_at: string
  updated_at: string
  deleted_at: string | null
}

export interface ContactListMember {
  id: string
  contact_list_id: string
  contact_id: string
  added_at: string
}

export interface ContactImportLog {
  id: string
  workspace_id: string
  total_rows: number
  imported: number
  updated: number
  skipped: number
  errors: number
  error_details: Array<{ row: number; reason: string }>
  created_at: string
}

export type ContactInsert = Omit<Contact, 'id' | 'created_at' | 'updated_at' | 'deleted_at'>
export type ContactUpdate = Partial<Omit<Contact, 'id' | 'workspace_id' | 'created_at' | 'updated_at' | 'deleted_at'>>
```

Update the `Database` interface to add contacts, contact_lists, contact_list_members, and contact_import_logs tables following the same Row/Insert/Update pattern used for profiles.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/database.ts contains "export interface Contact {"
    - src/types/database.ts contains "export interface ContactList {"
    - src/types/database.ts contains "export interface ContactListMember {"
    - src/types/database.ts contains "export interface ContactImportLog {"
    - src/types/database.ts contains "export type ContactInsert ="
    - src/types/database.ts contains "export type ContactUpdate ="
    - src/types/database.ts contains "status: 'active' | 'unsubscribed' | 'bounced' | 'complained'"
    - src/types/database.ts contains "tags: string[]"
    - src/types/database.ts contains "custom_fields: Record<string, string>"
    - The existing Profile and Database interfaces are preserved (not deleted)
    - TypeScript compilation succeeds (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>All four interfaces and two utility types exist in database.ts, TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create useContacts and useContactLists data hooks</name>
  <files>src/hooks/contacts/useContacts.ts, src/hooks/contacts/useContactLists.ts</files>
  <read_first>
    - src/types/database.ts (the just-updated file with Contact, ContactList types)
    - src/hooks/useAuth.ts (pattern for custom hooks — useAuth throws if no context)
    - src/lib/supabase.ts (Supabase client import path)
    - src/contexts/AuthContext.tsx (profile.workspace_id access pattern)
    - .planning/phases/01-contact-lists/01-RESEARCH.md (Pattern 3: Supabase Contact Query with Filters)
  </read_first>
  <action>
Create `src/hooks/contacts/useContacts.ts`:

This hook accepts a `filters` parameter object and returns `{ contacts, loading, error, refetch, totalCount }`.

```typescript
import { useState, useEffect, useCallback } from 'react'
import { supabase } from '../../lib/supabase'
import { useAuth } from '../useAuth'
import type { Contact } from '../../types/database'

export interface ContactFilters {
  search?: string
  status?: string
  tag?: string
  customFieldKey?: string
  customFieldValue?: string
  listId?: string
  page?: number
  pageSize?: number
}

export function useContacts(filters: ContactFilters = {}) {
  const { profile } = useAuth()
  const [contacts, setContacts] = useState<Contact[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)
  const [totalCount, setTotalCount] = useState(0)

  const pageSize = filters.pageSize ?? 50
  const page = filters.page ?? 1

  const fetchContacts = useCallback(async () => {
    if (!profile?.workspace_id) return
    setLoading(true)
    setError(null)

    try {
      // If filtering by list, first get contact IDs in that list
      let listContactIds: string[] | null = null
      if (filters.listId) {
        const { data: members } = await supabase
          .from('contact_list_members')
          .select('contact_id')
          .eq('contact_list_id', filters.listId)
        listContactIds = (members ?? []).map((m: { contact_id: string }) => m.contact_id)
        if (listContactIds.length === 0) {
          setContacts([])
          setTotalCount(0)
          setLoading(false)
          return
        }
      }

      let query = supabase
        .from('contacts')
        .select('*', { count: 'exact' })
        .eq('workspace_id', profile.workspace_id)
        .is('deleted_at', null)
        .order('created_at', { ascending: false })

      if (filters.search) {
        const safe = filters.search.trim()
        query = query.or(
          `email.ilike.%${safe}%,first_name.ilike.%${safe}%,last_name.ilike.%${safe}%`
        )
      }

      if (filters.status) {
        query = query.eq('status', filters.status)
      }

      if (filters.tag) {
        query = query.contains('tags', [filters.tag])
      }

      if (filters.customFieldKey && filters.customFieldValue) {
        query = query.contains('custom_fields', {
          [filters.customFieldKey]: filters.customFieldValue,
        })
      }

      if (listContactIds) {
        query = query.in('id', listContactIds)
      }

      // Pagination
      const from = (page - 1) * pageSize
      const to = from + pageSize - 1
      query = query.range(from, to)

      const { data, error: fetchError, count } = await query

      if (fetchError) {
        setError(fetchError.message)
      } else {
        setContacts((data as Contact[]) ?? [])
        setTotalCount(count ?? 0)
      }
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Failed to fetch contacts')
    } finally {
      setLoading(false)
    }
  }, [profile?.workspace_id, filters.search, filters.status, filters.tag, filters.customFieldKey, filters.customFieldValue, filters.listId, page, pageSize])

  useEffect(() => {
    fetchContacts()
  }, [fetchContacts])

  return { contacts, loading, error, refetch: fetchContacts, totalCount }
}
```

Create `src/hooks/contacts/useContactLists.ts`:

This hook returns `{ lists, loading, error, refetch, createList, updateList, deleteList }`.

```typescript
import { useState, useEffect, useCallback } from 'react'
import { supabase } from '../../lib/supabase'
import { useAuth } from '../useAuth'
import type { ContactList } from '../../types/database'

export function useContactLists() {
  const { profile } = useAuth()
  const [lists, setLists] = useState<ContactList[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  const fetchLists = useCallback(async () => {
    if (!profile?.workspace_id) return
    setLoading(true)
    setError(null)

    const { data, error: fetchError } = await supabase
      .from('contact_lists')
      .select('*')
      .eq('workspace_id', profile.workspace_id)
      .is('deleted_at', null)
      .order('created_at', { ascending: false })

    if (fetchError) {
      setError(fetchError.message)
    } else {
      setLists((data as ContactList[]) ?? [])
    }
    setLoading(false)
  }, [profile?.workspace_id])

  useEffect(() => {
    fetchLists()
  }, [fetchLists])

  const createList = async (name: string, description: string | null, color: string | null) => {
    if (!profile?.workspace_id) return { error: 'Not authenticated' }
    const { error } = await supabase.from('contact_lists').insert({
      workspace_id: profile.workspace_id,
      name,
      description,
      color,
    })
    if (error) return { error: error.message }
    await fetchLists()
    return { error: null }
  }

  const updateList = async (id: string, updates: { name?: string; description?: string | null; color?: string | null }) => {
    if (!profile?.workspace_id) return { error: 'Not authenticated' }
    const { error } = await supabase
      .from('contact_lists')
      .update({ ...updates, updated_at: new Date().toISOString() })
      .eq('id', id)
      .eq('workspace_id', profile.workspace_id)
    if (error) return { error: error.message }
    await fetchLists()
    return { error: null }
  }

  const deleteList = async (id: string) => {
    if (!profile?.workspace_id) return { error: 'Not authenticated' }
    const { error } = await supabase
      .from('contact_lists')
      .update({ deleted_at: new Date().toISOString() })
      .eq('id', id)
      .eq('workspace_id', profile.workspace_id)
    if (error) return { error: error.message }
    await fetchLists()
    return { error: null }
  }

  return { lists, loading, error, refetch: fetchLists, createList, updateList, deleteList }
}
```

Both hooks MUST:
- Import `supabase` from `'../../lib/supabase'`
- Import `useAuth` from `'../useAuth'`
- Always include `.eq('workspace_id', profile.workspace_id)` on every query
- Always include `.is('deleted_at', null)` on every SELECT query
- Use soft delete (update deleted_at) not hard delete
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/contacts/useContacts.ts contains "export function useContacts"
    - src/hooks/contacts/useContacts.ts contains "eq('workspace_id', profile.workspace_id)"
    - src/hooks/contacts/useContacts.ts contains ".is('deleted_at', null)"
    - src/hooks/contacts/useContacts.ts contains "export interface ContactFilters"
    - src/hooks/contacts/useContacts.ts contains "filters.search" and "filters.status" and "filters.tag" and "filters.listId"
    - src/hooks/contacts/useContacts.ts contains ".range(from, to)"
    - src/hooks/contacts/useContactLists.ts contains "export function useContactLists"
    - src/hooks/contacts/useContactLists.ts contains "eq('workspace_id', profile.workspace_id)"
    - src/hooks/contacts/useContactLists.ts contains ".is('deleted_at', null)"
    - src/hooks/contacts/useContactLists.ts contains "const createList"
    - src/hooks/contacts/useContactLists.ts contains "const deleteList"
    - src/hooks/contacts/useContactLists.ts contains "deleted_at: new Date().toISOString()"
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Both hooks exist, compile, and provide full CRUD + filtered query capabilities against Supabase with proper workspace scoping and soft-delete filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase PostgREST | User input (search, filter values) interpolated into Supabase query chains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | useContacts search filter | mitigate | Trim search input before interpolation into .or() PostgREST filter string; RLS enforces workspace isolation regardless |
| T-02-02 | Elevation of Privilege | All hooks | mitigate | Every query chains .eq('workspace_id', profile.workspace_id); RLS is the backup if app code is bypassed |
| T-02-03 | Information Disclosure | useContacts soft-delete filter | mitigate | Every SELECT query includes .is('deleted_at', null) to exclude soft-deleted records |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Both hooks export named functions
- Both hooks include workspace_id and deleted_at filters
- ContactFilters interface supports search, status, tag, customFieldKey, customFieldValue, listId
</verification>

<success_criteria>
- All four TypeScript interfaces match schema-v1.md column types exactly
- useContacts supports all filter combinations (CONT-06 through CONT-09) with pagination
- useContactLists supports create/update/soft-delete operations (LIST-01, LIST-02)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-contact-lists/01-02-SUMMARY.md`
</output>
