---
phase: 02-campaign-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/database.ts
  - src/lib/constants.ts
  - src/hooks/campaigns/useCampaigns.ts
  - src/hooks/campaigns/useCampaign.ts
  - src/App.tsx
autonomous: true
requirements: [CAMP-01, CAMP-07, CAMP-08]
must_haves:
  truths:
    - "Campaign TypeScript types match schema-v1.md Module 3 campaigns table exactly"
    - "useCampaigns hook can list, create, update, soft-delete, and duplicate campaigns"
    - "useCampaign hook can fetch a single campaign by ID and update it"
    - "TIMEZONES constant is importable from src/lib/constants.ts"
    - "Routes /campaigns, /campaigns/new, /campaigns/:id/edit are registered in App.tsx"
  artifacts:
    - path: "src/types/database.ts"
      provides: "Campaign interface, CampaignStatus type, CampaignInsert, CampaignUpdate types, campaigns table in Database interface"
      contains: "CampaignStatus"
    - path: "src/lib/constants.ts"
      provides: "TIMEZONES constant extracted from ProfilePage"
      exports: ["TIMEZONES"]
    - path: "src/hooks/campaigns/useCampaigns.ts"
      provides: "Campaign list hook with CRUD + duplicate"
      exports: ["useCampaigns"]
    - path: "src/hooks/campaigns/useCampaign.ts"
      provides: "Single campaign fetch/update hook"
      exports: ["useCampaign"]
    - path: "src/App.tsx"
      provides: "Campaign routes wired into protected layout"
  key_links:
    - from: "src/hooks/campaigns/useCampaigns.ts"
      to: "src/types/database.ts"
      via: "imports Campaign, CampaignInsert types"
      pattern: "import.*Campaign.*from.*database"
    - from: "src/hooks/campaigns/useCampaign.ts"
      to: "src/types/database.ts"
      via: "imports Campaign type"
      pattern: "import.*Campaign.*from.*database"
---

<objective>
Campaign foundation: TypeScript types, data hooks, route wiring, and shared constants.

Purpose: Establish the data layer and routing that all subsequent campaign plans depend on. Without types and hooks, no UI can be built. Without routes, no pages can be navigated to.
Output: Campaign types in database.ts, useCampaigns + useCampaign hooks, TIMEZONES in constants.ts, routes in App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-campaign-builder/02-CONTEXT.md
@.planning/phases/02-campaign-builder/02-RESEARCH.md
@.planning/phases/02-campaign-builder/02-UI-SPEC.md
@docs/schema-v1.md
@src/types/database.ts
@src/hooks/contacts/useContactLists.ts
@src/App.tsx
@src/pages/settings/ProfilePage.tsx

<interfaces>
<!-- Existing types to extend -->
From src/types/database.ts:
```typescript
export interface Database {
  public: {
    Tables: {
      profiles: { Row: Profile; Insert: ...; Update: ... }
      contacts: { Row: Contact; Insert: ContactInsert; Update: ContactUpdate }
      contact_lists: { Row: ContactList; Insert: ...; Update: ... }
      contact_list_members: { Row: ContactListMember; Insert: ...; Update: ... }
      contact_import_logs: { Row: ContactImportLog; Insert: ...; Update: never }
    }
  }
}
```

From src/hooks/contacts/useContactLists.ts (pattern to follow):
```typescript
export function useContactLists() {
  // Returns: { lists, loading, error, refetch, createList, updateList, deleteList }
  // Uses: useAuth() for profile.workspace_id, supabase client, soft-delete pattern
}
```

From src/App.tsx:
```typescript
// Protected routes under <AppLayout />:
<Route path="/contacts" element={<ContactsPage />} />
<Route path="/campaigns" element={<PlaceholderPage title="Campaigns" />} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaign types + TIMEZONES constant + route wiring</name>
  <files>src/types/database.ts, src/lib/constants.ts, src/App.tsx, src/pages/settings/ProfilePage.tsx</files>
  <read_first>
    - src/types/database.ts (current types to extend)
    - docs/schema-v1.md (Module 3 campaigns table DDL — source of truth for column names and types)
    - src/App.tsx (current route structure)
    - src/pages/settings/ProfilePage.tsx (TIMEZONES constant to extract)
  </read_first>
  <action>
**1. Add Campaign types to src/types/database.ts:**

Add after the existing ContactImportLog interface:

```typescript
export type CampaignStatus = 'draft' | 'scheduled' | 'sending' | 'sent' | 'paused' | 'cancelled'

export interface Campaign {
  id: string
  workspace_id: string
  name: string
  status: CampaignStatus
  from_name: string
  from_email: string
  reply_to_email: string | null
  subject: string
  preview_text: string | null
  body_html: string
  body_json: Record<string, unknown> | null
  contact_list_id: string | null
  segment_filter: Record<string, unknown> | null
  scheduled_at: string | null
  sent_at: string | null
  total_recipients: number
  total_sent: number
  total_delivered: number
  total_opened: number
  total_clicked: number
  total_replied: number
  total_bounced: number
  total_unsubscribed: number
  settings: Record<string, unknown>
  created_at: string
  updated_at: string
  deleted_at: string | null
}

export type CampaignInsert = Omit<Campaign, 'id' | 'total_recipients' | 'total_sent' | 'total_delivered' | 'total_opened' | 'total_clicked' | 'total_replied' | 'total_bounced' | 'total_unsubscribed' | 'created_at' | 'updated_at' | 'deleted_at' | 'sent_at'>

export type CampaignUpdate = Partial<Omit<Campaign, 'id' | 'workspace_id' | 'created_at' | 'updated_at' | 'deleted_at'>>
```

Add `campaigns` table to the Database interface:

```typescript
campaigns: {
  Row: Campaign
  Insert: CampaignInsert
  Update: CampaignUpdate
}
```

**2. Create src/lib/constants.ts:**

Extract the TIMEZONES array from src/pages/settings/ProfilePage.tsx into a shared module:

```typescript
export const TIMEZONES = [
  'UTC',
  'America/New_York',
  'America/Chicago',
  'America/Denver',
  'America/Los_Angeles',
  'America/Anchorage',
  'Pacific/Honolulu',
  'Europe/London',
  'Europe/Berlin',
  'Europe/Paris',
  'Asia/Tokyo',
  'Asia/Shanghai',
  'Asia/Kolkata',
  'Australia/Sydney',
  'Pacific/Auckland',
]
```

Copy the exact values from ProfilePage.tsx. Then update ProfilePage.tsx to import from the new file: `import { TIMEZONES } from '../../lib/constants'` and remove the local `const TIMEZONES` declaration.

**3. Wire campaign routes in src/App.tsx:**

Add imports at top:
```typescript
import { CampaignsPage } from './pages/campaigns/CampaignsPage'
import { CampaignBuilderPage } from './pages/campaigns/CampaignBuilderPage'
```

Replace the existing placeholder campaign route:
```typescript
<Route path="/campaigns" element={<PlaceholderPage title="Campaigns" />} />
```

With three routes:
```typescript
<Route path="/campaigns" element={<CampaignsPage />} />
<Route path="/campaigns/new" element={<CampaignBuilderPage />} />
<Route path="/campaigns/:id/edit" element={<CampaignBuilderPage />} />
```

Note: CampaignsPage and CampaignBuilderPage do not exist yet. Create minimal stub files so TypeScript does not error:

Create `src/pages/campaigns/CampaignsPage.tsx`:
```typescript
export function CampaignsPage() {
  return <div>Campaigns</div>
}
```

Create `src/pages/campaigns/CampaignBuilderPage.tsx`:
```typescript
export function CampaignBuilderPage() {
  return <div>Campaign Builder</div>
}
```

These stubs will be replaced in Plans 03 and 04.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/types/database.ts contains `export type CampaignStatus = 'draft' | 'scheduled' | 'sending' | 'sent' | 'paused' | 'cancelled'`
    - src/types/database.ts contains `export interface Campaign {`
    - src/types/database.ts contains `export type CampaignInsert =`
    - src/types/database.ts contains `export type CampaignUpdate =`
    - src/types/database.ts contains `campaigns:` inside the Database interface
    - src/lib/constants.ts contains `export const TIMEZONES =`
    - src/pages/settings/ProfilePage.tsx contains `import { TIMEZONES } from '../../lib/constants'`
    - src/pages/settings/ProfilePage.tsx does NOT contain `const TIMEZONES =`
    - src/App.tsx contains `import { CampaignsPage }` and `import { CampaignBuilderPage }`
    - src/App.tsx contains `path="/campaigns/new"`
    - src/App.tsx contains `path="/campaigns/:id/edit"`
    - src/App.tsx does NOT contain `PlaceholderPage title="Campaigns"`
    - src/pages/campaigns/CampaignsPage.tsx exists
    - src/pages/campaigns/CampaignBuilderPage.tsx exists
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Campaign types match schema-v1.md Module 3. TIMEZONES extracted and ProfilePage updated. Three campaign routes wired with stub pages. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: useCampaigns and useCampaign data hooks</name>
  <files>src/hooks/campaigns/useCampaigns.ts, src/hooks/campaigns/useCampaign.ts</files>
  <read_first>
    - src/hooks/contacts/useContactLists.ts (pattern to follow: useAuth, supabase queries, soft-delete, workspace_id filter)
    - src/types/database.ts (Campaign, CampaignInsert, CampaignUpdate types — just added in Task 1)
    - src/lib/supabase.ts (supabase client import path)
    - src/hooks/useAuth.ts (useAuth hook import path — or src/contexts/AuthContext.tsx if useAuth is exported from there)
  </read_first>
  <action>
**1. Create src/hooks/campaigns/useCampaigns.ts:**

Follow the exact pattern from useContactLists.ts. The hook returns `{ campaigns, loading, error, refetch, createCampaign, updateCampaign, deleteCampaign, duplicateCampaign }`.

```typescript
import { useState, useEffect, useCallback } from 'react'
import { supabase } from '../../lib/supabase'
import { useAuth } from '../useAuth'
import type { Campaign, CampaignInsert, CampaignUpdate } from '../../types/database'

export function useCampaigns() {
  const { profile } = useAuth()
  const [campaigns, setCampaigns] = useState<Campaign[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  const fetchCampaigns = useCallback(async () => {
    if (!profile?.workspace_id) return
    setLoading(true)
    setError(null)

    const { data, error: fetchError } = await supabase
      .from('campaigns')
      .select('*')
      .eq('workspace_id', profile.workspace_id)
      .is('deleted_at', null)
      .order('created_at', { ascending: false })

    if (fetchError) {
      setError(fetchError.message)
    } else {
      setCampaigns((data as Campaign[]) ?? [])
    }
    setLoading(false)
  }, [profile?.workspace_id])

  useEffect(() => {
    fetchCampaigns()
  }, [fetchCampaigns])

  const createCampaign = async (campaign: Omit<CampaignInsert, 'workspace_id'>) => {
    if (!profile?.workspace_id) return { data: null, error: 'Not authenticated' }
    const { data, error } = await supabase
      .from('campaigns')
      .insert({ ...campaign, workspace_id: profile.workspace_id })
      .select()
      .single()
    if (error) return { data: null, error: error.message }
    await fetchCampaigns()
    return { data: data as Campaign, error: null }
  }

  const updateCampaign = async (id: string, updates: CampaignUpdate) => {
    if (!profile?.workspace_id) return { error: 'Not authenticated' }
    const { error } = await supabase
      .from('campaigns')
      .update({ ...updates, updated_at: new Date().toISOString() })
      .eq('id', id)
      .eq('workspace_id', profile.workspace_id)
    if (error) return { error: error.message }
    await fetchCampaigns()
    return { error: null }
  }

  const deleteCampaign = async (id: string) => {
    if (!profile?.workspace_id) return { error: 'Not authenticated' }
    const { error } = await supabase
      .from('campaigns')
      .update({ deleted_at: new Date().toISOString() })
      .eq('id', id)
      .eq('workspace_id', profile.workspace_id)
    if (error) return { error: error.message }
    await fetchCampaigns()
    return { error: null }
  }

  const duplicateCampaign = async (id: string) => {
    if (!profile?.workspace_id) return { data: null, error: 'Not authenticated' }
    // Fetch the source campaign
    const source = campaigns.find(c => c.id === id)
    if (!source) return { data: null, error: 'Campaign not found' }
    // Create a copy with "Copy of " prefix and draft status
    const { data, error } = await supabase
      .from('campaigns')
      .insert({
        workspace_id: profile.workspace_id,
        name: `Copy of ${source.name}`,
        status: 'draft',
        from_name: source.from_name,
        from_email: source.from_email,
        reply_to_email: source.reply_to_email,
        subject: source.subject,
        preview_text: source.preview_text,
        body_html: source.body_html,
        body_json: source.body_json,
        contact_list_id: source.contact_list_id,
        segment_filter: source.segment_filter,
        scheduled_at: null,
        settings: source.settings,
      })
      .select()
      .single()
    if (error) return { data: null, error: error.message }
    await fetchCampaigns()
    return { data: data as Campaign, error: null }
  }

  return { campaigns, loading, error, refetch: fetchCampaigns, createCampaign, updateCampaign, deleteCampaign, duplicateCampaign }
}
```

**2. Create src/hooks/campaigns/useCampaign.ts:**

Single-campaign hook for the builder page. Fetches by ID and provides update.

```typescript
import { useState, useEffect, useCallback } from 'react'
import { supabase } from '../../lib/supabase'
import { useAuth } from '../useAuth'
import type { Campaign, CampaignUpdate } from '../../types/database'

export function useCampaign(id: string | undefined) {
  const { profile } = useAuth()
  const [campaign, setCampaign] = useState<Campaign | null>(null)
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  const fetchCampaign = useCallback(async () => {
    if (!profile?.workspace_id || !id) {
      setLoading(false)
      return
    }
    setLoading(true)
    setError(null)

    const { data, error: fetchError } = await supabase
      .from('campaigns')
      .select('*')
      .eq('id', id)
      .eq('workspace_id', profile.workspace_id)
      .is('deleted_at', null)
      .single()

    if (fetchError) {
      setError(fetchError.message)
      setCampaign(null)
    } else {
      setCampaign(data as Campaign)
    }
    setLoading(false)
  }, [profile?.workspace_id, id])

  useEffect(() => {
    fetchCampaign()
  }, [fetchCampaign])

  const updateCampaign = async (updates: CampaignUpdate) => {
    if (!profile?.workspace_id || !id) return { error: 'Not authenticated' }
    const { error } = await supabase
      .from('campaigns')
      .update({ ...updates, updated_at: new Date().toISOString() })
      .eq('id', id)
      .eq('workspace_id', profile.workspace_id)
    if (error) return { error: error.message }
    await fetchCampaign()
    return { error: null }
  }

  return { campaign, loading, error, refetch: fetchCampaign, updateCampaign }
}
```

Verify the import path for useAuth by reading the actual file. If useAuth is exported from `src/contexts/AuthContext.tsx` rather than `src/hooks/useAuth.ts`, use the correct path. The useContactLists.ts reference shows `import { useAuth } from '../useAuth'` which means the hook file is at `src/hooks/useAuth.ts` (since useContactLists is in `src/hooks/contacts/`). Campaign hooks are in `src/hooks/campaigns/`, so the import should also be `'../useAuth'`.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/campaigns/useCampaigns.ts contains `export function useCampaigns()`
    - src/hooks/campaigns/useCampaigns.ts contains `const fetchCampaigns`
    - src/hooks/campaigns/useCampaigns.ts contains `const createCampaign`
    - src/hooks/campaigns/useCampaigns.ts contains `const duplicateCampaign`
    - src/hooks/campaigns/useCampaigns.ts contains `Copy of ${source.name}`
    - src/hooks/campaigns/useCampaigns.ts contains `.eq('workspace_id', profile.workspace_id)`
    - src/hooks/campaigns/useCampaigns.ts contains `.is('deleted_at', null)`
    - src/hooks/campaigns/useCampaign.ts contains `export function useCampaign(`
    - src/hooks/campaigns/useCampaign.ts contains `.eq('workspace_id', profile.workspace_id)`
    - src/hooks/campaigns/useCampaign.ts contains `const updateCampaign`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>useCampaigns hook provides list/create/update/delete/duplicate with workspace_id filtering and soft-delete. useCampaign hook provides single-campaign fetch and update. Both compile clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase | All campaign queries must filter by workspace_id to prevent cross-workspace data leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | useCampaigns, useCampaign | mitigate | Every Supabase query includes `.eq('workspace_id', profile.workspace_id)` + `.is('deleted_at', null)` — enforced in hook code. RLS policies on campaigns table provide server-side defense-in-depth. |
| T-2-05 | Tampering | CampaignUpdate type | mitigate | CampaignUpdate omits `id`, `workspace_id`, `created_at`, `updated_at`, `deleted_at` — prevents client from overwriting immutable fields. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (all new types and hooks compile)
- `npm run lint` passes
- Campaign interface has all 27 fields from schema-v1.md Module 3
- TIMEZONES importable from src/lib/constants.ts
- Routes /campaigns, /campaigns/new, /campaigns/:id/edit exist in App.tsx
</verification>

<success_criteria>
Campaign data layer is complete. Subsequent plans can import Campaign types, use hooks for CRUD, and navigate to campaign routes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-campaign-builder/02-01-SUMMARY.md`
</output>
