---
phase: 08-email-signature-rich-html-body
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified: []
autonomous: false
requirements:
  - SIG-01
  - SIG-02
  - SIG-03
  - SIG-04
  - SIG-05
  - CLR-01
  - ALN-01

must_haves:
  truths:
    - "Database schema has signature_html and signature_json columns on profiles"
    - "Application builds without TypeScript errors"
    - "Application passes lint checks"
  artifacts:
    - path: "supabase/migrations/010_signature.sql"
      provides: "Applied migration"
      contains: "ALTER TABLE"
  key_links:
    - from: "supabase/migrations/010_signature.sql"
      to: "Supabase live database"
      via: "supabase db push or SQL Editor"
      pattern: "signature_html"
---

<objective>
Push the database schema migration to live Supabase and run full build + lint verification to confirm all phase changes compile and pass.

Purpose: [BLOCKING] The phase cannot pass verification without the schema push. TypeScript types and Edge Functions reference signature_html/signature_json columns that must exist in the live database.
Output: Live database has 010_signature migration applied. Build and lint pass cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-email-signature-rich-html-body/08-CONTEXT.md
@.planning/phases/08-email-signature-rich-html-body/08-01-SUMMARY.md
@.planning/phases/08-email-signature-rich-html-body/08-02-SUMMARY.md
@.planning/phases/08-email-signature-rich-html-body/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push database schema migration 010</name>
  <files></files>
  <read_first>
    supabase/migrations/010_signature.sql
  </read_first>
  <action>
Attempt to push the schema migration using the Supabase CLI:
```bash
cd "/Users/lucaspaiva/vibe coding/dna/mailsops"
supabase db push
```

**If the CLI fails due to non-TTY environment** (no SUPABASE_ACCESS_TOKEN configured — identical to Phase 06 and Phase 07 pattern per STATE.md decision):
1. Read the migration file contents: `cat supabase/migrations/010_signature.sql`
2. Present the SQL to the user for manual execution via the Supabase SQL Editor
3. The SQL to execute manually:
```sql
ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS signature_html TEXT,
  ADD COLUMN IF NOT EXISTS signature_json JSONB;
```
4. Wait for user confirmation that the migration was applied

**Verification after push/manual apply:**
Check that the columns exist by querying the database schema (if CLI is available):
```bash
supabase db diff --schema public
```
Or the user confirms the SQL executed successfully in the SQL Editor.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && echo "Schema push verification: user confirms migration 010 applied"</automated>
  </verify>
  <acceptance_criteria>
    - Migration 010_signature.sql has been applied to the live Supabase database
    - profiles table has signature_html TEXT column
    - profiles table has signature_json JSONB column
    - User has confirmed the migration was successful
  </acceptance_criteria>
  <done>Migration 010 applied to live Supabase. profiles table has both signature columns.</done>
</task>

<task type="auto">
  <name>Task 2: Full build and lint verification</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the full build and lint suite to verify all phase changes compile cleanly:

```bash
cd "/Users/lucaspaiva/vibe coding/dna/mailsops"
npm run lint 2>&1
npm run build 2>&1
```

Both commands must exit with code 0. If there are errors:
1. Read the error output carefully
2. Fix any TypeScript compilation errors, lint issues, or import problems
3. Re-run until both pass

Expected: zero errors since all TypeScript types, imports, and TipTap extension registrations were handled in Plans 01-03.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "npm run lint" exits with code 0 (no lint errors)
    - "npm run build" exits with code 0 (no TypeScript compilation errors)
    - Build output directory (dist/) is generated successfully
  </acceptance_criteria>
  <done>Full build and lint pass. All phase 8 changes compile correctly. Phase is ready for /gsd-verify-work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional smoke test</name>
  <files></files>
  <action>
User verifies the complete Phase 8 implementation:

1. Open the app and navigate to Settings > Workspace tab
2. Verify the "Email Signature" section appears below "Sending Defaults" with a TipTap editor
3. Type a test signature (e.g., "Best regards, {{first_name}}") and use formatting (bold, color, alignment)
4. Click "Save workspace settings" -- verify success toast appears
5. Reload the page -- verify the signature content persists in the editor
6. Navigate to Campaigns > create or edit a campaign
7. Switch to Preview tab -- verify the signature appears below the email body with an hr separator
8. Verify the toolbar now shows alignment buttons (after H1/H2) and a color picker (paint palette icon after Image)
9. Test the color picker: click Palette icon, verify 8 color swatches appear in a 4x2 grid
10. Click a color -- verify it applies to selected text
11. Test alignment: select text, click center/right alignment, verify it changes
12. (Optional) Send a test email and verify the signature appears in the received email
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has approved the visual and functional verification of all Phase 8 features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> Supabase API | Schema push requires authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Elevation | Schema migration | accept | ALTER TABLE ADD COLUMN IF NOT EXISTS is additive only — no data loss risk. Existing RLS policies on profiles table apply to new columns automatically. |
</threat_model>

<verification>
1. Migration 010 applied to live database
2. `npm run lint` exits 0
3. `npm run build` exits 0
4. User visual verification of signature editor, toolbar, and preview
</verification>

<success_criteria>
- Live database has signature_html and signature_json columns
- Build and lint pass with zero errors
- User has approved the visual and functional verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-email-signature-rich-html-body/08-04-SUMMARY.md`
</output>
