---
phase: 07-templates-settings
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified: []
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04

must_haves:
  truths:
    - "Database schema is pushed to live Supabase (templates table + profile columns exist)"
    - "Full application builds without errors"
    - "Lint passes with no errors"
  artifacts: []
  key_links: []
---

<objective>
Push the database schema migration to live Supabase and perform full build/lint verification. This is a blocking gate -- the application cannot function against the live database without the schema push.

Purpose: Previous phases have shown that `supabase db push` fails in non-TTY environments (no SUPABASE_ACCESS_TOKEN). This plan handles the schema push via manual SQL Editor if needed, then verifies the full application compiles.
Output: Live database has templates table and profiles extensions. Build and lint are green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-templates-settings/07-CONTEXT.md
@.planning/phases/07-templates-settings/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt schema push and run build/lint verification</name>
  <files></files>
  <read_first>
    - supabase/migrations/009_templates_settings.sql
  </read_first>
  <action>
Attempt to push the schema using the Supabase CLI:

```bash
cd "/Users/lucaspaiva/vibe coding/dna/mailsops"
npx supabase db push
```

If this succeeds, proceed to build/lint verification.

If this fails due to non-TTY environment or SUPABASE_ACCESS_TOKEN not being set (which is the pattern from Phase 03, Phase 05, and Phase 06 -- see STATE.md decision about CLI push failing), then:

1. Display the full contents of `supabase/migrations/009_templates_settings.sql` to the user
2. Instruct the user to execute the SQL manually via the Supabase SQL Editor at:
   `https://supabase.com/dashboard/project/pozqnzhgqmajtaidtpkk/sql/new`
3. Wait for user confirmation that the SQL has been executed

After schema push (CLI or manual), run verification:
```bash
npm run build
npm run lint
```

Both must pass with zero errors.
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npm run build 2>&1 | tail -3 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Schema push completes via CLI OR user confirms manual execution via SQL Editor
    - templates table exists in live Supabase database
    - profiles table has default_sender_name, default_sender_email, resend_api_key, unsubscribe_footer_text columns
    - `npm run build` exits with code 0
    - `npm run lint` exits with code 0
  </acceptance_criteria>
  <done>Database schema for Phase 7 is live in Supabase. Build and lint pass with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Full-phase smoke test verification</name>
  <files></files>
  <action>
This task is a human-verify checkpoint. Present the following verification checklist to the user and wait for confirmation.

Verification steps (user performs in browser):
1. Start dev server with `npm run dev` if not already running
2. Navigate to `/templates` -- should show empty state "No templates yet" with FileText icon
3. Navigate to `/campaigns` -- click "..." on any campaign row -- "Save as template" option should appear in dropdown
4. Click "Save as template" -- modal should open with campaign name pre-filled
5. Save a template -- Toast "Template saved." should appear
6. Navigate to `/templates` -- saved template should appear in table with name, subject, date
7. Click "..." on the template row -- "Use template" and "Delete" actions should appear
8. Click "Use template" -- should navigate to `/campaigns/new?from_template=...` with fields pre-filled
9. Navigate to `/settings` -- should show tabbed page with Profile tab active by default
10. Click Workspace tab -- should show "Sending Defaults" with sender name and email inputs
11. Click Integrations tab -- should show "Resend Integration" with API key (masked), sending domain ("resend.dev shared domain" with Active badge), and unsubscribe footer textarea
12. Navigate to `/settings/profile` -- should redirect to `/settings`
13. Verify sidebar "Settings" link highlights correctly when on /settings

Ask user: "Type 'approved' or describe any issues."
  </action>
  <verify>
    <automated>echo "Manual verification -- user approval required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 13 verification steps pass
    - OR user describes issues which are then fixed before re-verification
  </acceptance_criteria>
  <done>Full Phase 7 smoke test passes. User has approved all features are working correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> Supabase | Schema push modifies production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering | Migration SQL | mitigate | Migration file is version controlled; SQL reviewed before push; RLS enabled on new table |
| T-07-15 | Denial of Service | Schema push failure | accept | Fallback to manual SQL Editor execution -- proven pattern from Phase 03/05/06 |
</threat_model>

<verification>
- `npm run build` passes with zero errors
- `npm run lint` passes with zero errors
- Database schema is live in Supabase
- Full manual smoke test approved by user
</verification>

<success_criteria>
- Schema pushed to live Supabase (templates table + 4 profile columns)
- `npm run build` green
- `npm run lint` green
- Full manual smoke test passes all 13 verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/07-templates-settings/07-05-SUMMARY.md`
</output>
