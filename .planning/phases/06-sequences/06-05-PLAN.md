---
phase: 06-sequences
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified: []
autonomous: false
requirements:
  - SEQN-01
  - SEQN-02
  - SEQN-03
  - SEQN-04

must_haves:
  truths:
    - "Database migration 008_sequences.sql is applied to live Supabase project"
    - "All four tables (sequences, sequence_steps, sequence_enrollments, sequence_step_sends) exist in production"
    - "RLS policies are active on all four tables"
    - "npm run build exits 0"
    - "npm run lint passes (or only pre-existing warnings)"
  artifacts: []
  key_links: []
---

<objective>
[BLOCKING] Apply the database schema migration to the live Supabase project and verify the full build. This is mandatory — the phase cannot pass verification without it.

Purpose: Without the live schema, all sequence operations will fail at runtime (CRUD, enrollment, stats queries). Build and type checks pass without it (types come from local files), creating a false-positive verification state.
Output: Live database tables ready for sequence operations.
</objective>

<execution_context>
@/Users/lucaspaiva/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lucaspaiva/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-sequences/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Attempt schema push via Supabase CLI</name>
  <files></files>
  <read_first>
    - supabase/migrations/008_sequences.sql
    - supabase/config.toml
  </read_first>
  <action>
Attempt to push the database schema using the Supabase CLI:

```bash
cd "/Users/lucaspaiva/vibe coding/dna/mailsops"
npx supabase db push
```

If this fails due to authentication (no SUPABASE_ACCESS_TOKEN, non-TTY login), try:
```bash
npx supabase link --project-ref pozqnzhgqmajtaidtpkk
npx supabase db push
```

If CLI authentication fails entirely, flag for manual intervention (Task 2 checkpoint). This matches the pattern from Phase 03 Plan 01 and Phase 05 Plan 01 where migrations were applied manually via the SQL Editor.

Also run the full verification suite:
```bash
npm run build && npm run lint
```
  </action>
  <verify>
    <automated>cd "/Users/lucaspaiva/vibe coding/dna/mailsops" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - npm run lint exits 0 (or only pre-existing warnings)
  </acceptance_criteria>
  <done>Build and lint pass. Schema push either succeeded via CLI or is flagged for manual application.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Manual schema application (if CLI push failed)</name>
  <files></files>
  <action>Human action required: If Task 1 CLI push failed, apply migration 008_sequences.sql manually via the Supabase SQL Editor. Enable pg_cron and pg_net extensions first if not already enabled. See how-to-verify for step-by-step instructions.</action>
  <what-built>Migration file 008_sequences.sql is ready. All frontend code compiles. If Task 1 CLI push succeeded, this step can be skipped.</what-built>
  <how-to-verify>
If the Supabase CLI push in Task 1 failed, apply the migration manually:

1. Open Supabase Dashboard: https://supabase.com/dashboard/project/pozqnzhgqmajtaidtpkk
2. Go to SQL Editor
3. Copy the contents of `supabase/migrations/008_sequences.sql`
4. **Important:** If pg_cron and pg_net extensions are not already enabled:
   - Go to Database -> Extensions -> search "pg_cron" -> Enable
   - Go to Database -> Extensions -> search "pg_net" -> Enable
5. Run the migration SQL in the SQL Editor
6. Verify all four tables were created:
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name IN ('sequences', 'sequence_steps', 'sequence_enrollments', 'sequence_step_sends');
```
Expected: 4 rows returned.

7. Verify RLS is enabled:
```sql
SELECT tablename, rowsecurity FROM pg_tables
WHERE schemaname = 'public'
AND tablename IN ('sequences', 'sequence_steps', 'sequence_enrollments', 'sequence_step_sends');
```
Expected: All 4 rows show `rowsecurity = true`.
  </how-to-verify>
  <resume-signal>Type "schema applied" when all 4 tables are confirmed in production, or "skipped" if CLI push already succeeded in Task 1.</resume-signal>
  <verify>
    <automated>echo "Checkpoint: manual verification required"</automated>
  </verify>
  <done>All 4 sequence tables exist in production Supabase with RLS enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL -> Supabase | DDL applied to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-15 | Tampering | migration application | accept | Migration is additive (CREATE TABLE only, no ALTER on existing tables). If it fails, existing tables are unaffected. Safe to re-run (IF NOT EXISTS on extensions). |
</threat_model>

<verification>
- All 4 tables exist in production Supabase
- RLS is enabled on all 4 tables
- npm run build exits 0
- npm run lint passes
</verification>

<success_criteria>
Database schema is live in production. All sequence CRUD, enrollment, and stats operations will work at runtime. Phase 6 is ready for end-to-end verification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sequences/06-05-SUMMARY.md`
</output>
