---
phase: 06
slug: sequences
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-14
---

# Phase 06 — UI Design Contract: Sequences

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind CSS v4 (no shadcn; `components.json` absent) |
| Preset | not applicable |
| Component library | none (custom components in `src/components/ui/`) |
| Icon library | lucide-react 0.511.0 |
| Font | System sans-serif (Tailwind default, no custom font declared) |

Source: codebase scan — `components.json` not found, `src/index.css` contains only `@import "tailwindcss"`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), badge padding (`py-0.5`) |
| sm | 8px | Compact element spacing (`gap-2`), button icon gap |
| md | 16px | Default element spacing (`gap-4`), section inner padding |
| lg | 24px | Section gaps (`gap-6`), card padding default (`p-6`) |
| xl | 32px | Builder section gaps (`gap-8`) |
| 2xl | 48px | Major section breaks, empty state padding (`py-16`) |
| 3xl | 64px | Page-level spacing (unused in existing pages, available) |

Exceptions:
- Step number badge: 28px min-width circle (`w-7 h-7`) — visual affordance, not a spacing token
- Nav icon touch target: 20px (`w-5 h-5`) within 40px (`px-3 py-2.5`) nav link hit area
- TipTap editor minimum height: 320px (`min-h-[320px]`) — functional constraint, not a spacing token

Source: established from existing pages (`CampaignsPage.tsx`, `AbTestBuilderPage.tsx`, `CampaignBuilderPage.tsx`).

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm` |
| Label | 14px | 500 (medium) | 1.4 | `text-sm font-medium` |
| Section heading | 12px | 600 (semibold) | 1.2 | `text-xs font-semibold uppercase tracking-wide` |
| Page heading | 20px | 600 (semibold) | 1.2 | `text-xl font-semibold` |

Notes:
- Body text: `text-gray-100` for primary content, `text-gray-300` for secondary, `text-gray-400` for metadata/labels
- Section headings always uppercase with `tracking-wide` — matches existing `Details`, `Shared Settings`, etc. sections
- TipTap editor body uses `text-sm leading-relaxed` (`leading-relaxed` = 1.625)
- Inline-editable sequence name input: `text-xl font-semibold` matching `CampaignBuilderPage` pattern

Source: extracted from `CampaignBuilderPage.tsx`, `AbTestBuilderPage.tsx`, `AbTestResultsPage.tsx`.

---

## Color

| Role | Tailwind Value | Hex (approx) | Usage |
|------|---------------|-------------|-------|
| Dominant (60%) | `bg-gray-950` | #030712 | App background (AppLayout) |
| Secondary (30%) | `bg-gray-900` | #111827 | Cards (`Card` default), sidebar, page content areas |
| Tertiary surface | `bg-gray-800` | #1f2937 | Input backgrounds, dropdown menus, toolbar |
| Accent (10%) | `indigo-600` / `indigo-400` | #4f46e5 / #818cf8 | See reserved list below |
| Destructive | `red-700` / `red-400` | #b91c1c / #f87171 | Archive/delete actions only |

Accent reserved for:
- Sidebar active nav item background (`bg-indigo-600/20`) and text (`text-indigo-400`)
- "Sequences" nav item active state (same pattern as existing nav items)
- Primary Button background (`bg-indigo-600 hover:bg-indigo-500`)
- Focus ring on inputs (`focus:ring-indigo-500`)
- Active tab indicator border (`border-indigo-500`) on step tab strip
- MailOps logo mark (`bg-indigo-600`)

Status badge colors (using existing `Badge` component variants — source: `Badge.tsx`):
- `draft` → `default` (gray) — `bg-gray-700 text-gray-200`
- `active` → `success` (green) — `bg-green-900/50 text-green-400`
- `paused` → `warning` (yellow) — `bg-yellow-900/50 text-yellow-400`
- `archived` → `default` (gray) — `bg-gray-700 text-gray-200`

Per-step stat icon colors (matching `AbTestResultsPage` pattern):
- Open Rate icon: `text-green-400`
- Sent icon: `text-gray-400`

Source: `Sidebar.tsx`, `Button.tsx`, `Badge.tsx`, `Card.tsx`, `AbTestResultsPage.tsx`.

---

## Sidebar Navigation

Add "Sequences" entry to `src/components/layout/Sidebar.tsx` `navItems` array between Campaigns (`/campaigns`) and Templates (`/templates`):

```
{ to: '/sequences', icon: Workflow, label: 'Sequences' }
```

Icon: `Workflow` from lucide-react (preferred per CONTEXT.md D-06; `ListOrdered` is acceptable alternative if `Workflow` is unavailable in installed version 0.511.0).

Source: CONTEXT.md D-06, `Sidebar.tsx`.

---

## Page Layouts

### SequencesPage (`/sequences`) — List Page

Layout mirrors `CampaignsPage.tsx` exactly:

```
flex flex-col gap-6
  ├── Page header row (flex items-center justify-between)
  │     ├── h1 "Sequences" (text-xl font-semibold text-gray-100)
  │     └── Button primary "New Sequence" (Plus icon + label)
  └── Card padding="sm" overflow-hidden p-0
        ├── [loading]  → centered Spinner size="lg" py-16
        ├── [empty]    → EmptyState (see Copywriting Contract)
        └── [data]     → <table> full-width
```

Table columns:
| Column | Content | Width |
|--------|---------|-------|
| Sequence | name (`text-sm font-semibold text-gray-100`) | auto |
| Status | `<Badge>` variant mapped from sequence status | auto |
| Contact list | list name or `—` (`text-sm text-gray-300`) | auto |
| Steps | step count + "steps" label (`text-sm text-gray-400`) | auto |
| Enrolled | enrollment count + "enrolled" label (`text-sm text-gray-400`) | auto |
| Actions | `MoreHorizontal` ghost button → dropdown menu | 48px |

Table header: `text-xs font-semibold text-gray-400 uppercase tracking-wide bg-gray-900 px-4 py-3 text-left`
Table rows: `border-b border-gray-800 hover:bg-gray-800/50 cursor-pointer`
Row click → navigate to `/sequences/:id/edit` (if draft/paused) or `/sequences/:id/results` (if active/archived)

Dropdown menu options (per sequence status):
- **Draft:** Edit, Delete
- **Active:** View Results, Pause, Archive
- **Paused:** View Results, Resume, Archive
- **Archived:** View Results (read-only; no delete if enrollments exist)

Delete confirmation: `window.confirm('Delete "{name}"? This cannot be undone.')` — matches `CampaignsPage` pattern.
Archive confirmation: `window.confirm('Archive "{name}"? Active enrollments will stop receiving steps.')` — inline confirm.

---

### SequenceBuilderPage (`/sequences/new`, `/sequences/:id/edit`) — Builder

Layout mirrors `AbTestBuilderPage.tsx` two-panel pattern:

```
flex flex-col gap-8 max-w-4xl
  ├── Page header (flex items-start justify-between gap-4 border-b border-gray-800 pb-4)
  │     ├── Inline name input (text-xl font-semibold — editable in place)
  │     └── Button group: [Save draft (secondary sm)] [Start Sequence (primary md)]
  ├── Card — Shared Settings
  │     ├── Section label "Sequence Settings" (text-xs uppercase tracking-wide)
  │     └── grid grid-cols-1 md:grid-cols-2 gap-4
  │           ├── From name (Input)
  │           ├── From email (Input type=email)
  │           ├── Reply-to (Input type=email, optional)
  │           └── Target list (select — same pattern as AbTestBuilderPage)
  ├── Section label "Steps" (text-xs uppercase tracking-wide)
  └── Steps area (flex flex-col gap-6)
        ├── [Step 1 panel]
        ├── [Step 2 panel]
        ├── ...
        └── Button ghost/secondary "Add Step" (Plus icon) — full width, border-dashed border-gray-700
```

Each step panel is a `Card` with `padding="md"`:

```
Card (border border-gray-700 rounded-lg p-6)
  ├── Step header row (flex items-center justify-between mb-4)
  │     ├── Step number badge (w-7 h-7 rounded-full bg-gray-800 text-gray-300 text-xs font-semibold)
  │     │   + label "Step N" (text-sm font-medium text-gray-300 ml-2)
  │     └── Remove button (X icon, ghost, size sm) — hidden if only 1 step
  ├── Delay input row (flex items-center gap-2 mb-4)
  │     ├── label "Send on day" (text-sm font-medium text-gray-300)
  │     ├── number input (w-20 bg-gray-800 border-gray-700 text-gray-100 text-sm rounded-lg px-3 py-2)
  │     └── "(days after enrollment)" (text-sm text-gray-400)
  ├── Subject row with variable button (same pattern as CampaignBuilderPage subject + {{ }} button)
  └── TipTap editor (border border-gray-700 rounded-lg overflow-hidden)
        ├── CampaignEditorToolbar (reused)
        └── EditorContent min-h-[280px] (slightly shorter than single-campaign 320px — multiple steps visible)
```

Step reorder UX: Up/Down arrow buttons (`ChevronUp`/`ChevronDown` from lucide-react, ghost size sm) placed in step header row, between step badge and remove button. First step hides ChevronUp; last step hides ChevronDown. Drag-and-drop is deferred.

Step validation: delay_days must be strictly increasing (step N delay_days > step N-1 delay_days). Inline error displayed beneath delay row: `text-sm text-red-400`.

"Start Sequence" button:
- Disabled if: sequence has 0 steps, any step is missing subject or has empty body, contact list not selected
- Requires confirmation modal (not `window.confirm`) — see Interaction States below
- After start: navigate to `/sequences/:id/results`

"Save draft" button: always available, no validation required. Saves current state silently.

---

### SequenceResultsPage (`/sequences/:id/results`) — Results

Layout mirrors `AbTestResultsPage.tsx` / `CampaignAnalyticsPage.tsx`:

```
flex flex-col gap-6 max-w-4xl
  ├── Header row
  │     ├── Back link (ArrowLeft → /sequences)
  │     ├── h1 "{sequence name} — Results" (text-xl font-semibold text-gray-100)
  │     └── Badge (status variant)
  ├── Enrollment summary Card (padding="md")
  │     └── text-sm text-gray-400 "{N} contacts enrolled · Status: Active/Paused"
  │         + Pause/Resume button (secondary sm, right-aligned)
  └── Per-step results (flex flex-col gap-6)
        ├── Step section heading "Step 1 — Day {delay_days}" (text-sm font-semibold text-gray-300 uppercase tracking-wide)
        └── StatCard grid (grid-cols-1 sm:grid-cols-3 gap-4)
              ├── StatCard label="Sent" value="{N}" icon=Send iconColor="text-gray-400"
              ├── StatCard label="Open Rate" value="{N}%" subLabel="{opened} of {sent}" icon=Eye iconColor="text-green-400"
              └── StatCard label="Click Rate" value="{N}%" subLabel="{clicked} of {sent}" icon=MousePointer iconColor="text-indigo-400"
```

Empty results state (step not yet sent):
- Render step section heading
- Render a single `Card` with `text-sm text-gray-400 text-center py-8`: "Step not sent yet. Stats will appear after this step runs."

---

## Component Inventory

Components to create (new):

| Component | Path | Reuses |
|-----------|------|--------|
| `SequencesPage` | `src/pages/sequences/SequencesPage.tsx` | `CampaignsPage` pattern |
| `SequenceBuilderPage` | `src/pages/sequences/SequenceBuilderPage.tsx` | `AbTestBuilderPage` pattern |
| `SequenceResultsPage` | `src/pages/sequences/SequenceResultsPage.tsx` | `AbTestResultsPage` + `CampaignAnalyticsPage` patterns |
| `StepEditorPanel` | `src/components/sequences/StepEditorPanel.tsx` | TipTap toolbar, subject input, VariableDropdown |
| `StartSequenceModal` | `src/components/sequences/StartSequenceModal.tsx` | `SendWinnerModal` pattern |
| `useSequences` | `src/hooks/sequences/useSequences.ts` | `useCampaigns` pattern |
| `useSequence` | `src/hooks/sequences/useSequence.ts` | `useCampaign` pattern |

Components reused (no modification):
- `Button`, `Card`, `Badge`, `Input`, `Spinner` from `src/components/ui/`
- `StatCard` from `src/components/analytics/StatCard.tsx`
- `CampaignEditorToolbar` from `src/components/campaigns/CampaignEditorToolbar.tsx`
- `VariableChipNode`, `VariableSlashCommand`, `VariableDropdown` from `src/components/campaigns/`

Components modified:
- `Sidebar.tsx` — add Sequences nav item (Workflow icon, `/sequences` route)
- `AppLayout.tsx` — add `/sequences` to `pageTitles` map as `'Sequences'`
- `App.tsx` — add route declarations for all four `/sequences/*` paths

---

## Interaction States

### Start Sequence — Confirmation Modal (`StartSequenceModal`)

Not `window.confirm`. Uses a custom modal matching `SendWinnerModal` pattern.

Modal content:
- Heading: "Start this sequence?"
- Body: "This will enroll {N} active contacts from {list name}. Emails will begin sending automatically. Contacts added after this point will not be enrolled."
- Actions: `[Cancel (secondary)]` `[Start Sequence (primary, loading state)]`

### Pause / Resume — Inline confirmation

Pause: `window.confirm('Pause this sequence? Active contacts will stop receiving steps until you resume.')` — acceptable because it is a recoverable action.
Resume: No confirmation required. Button label: "Resume" (primary sm).

### Delete — `window.confirm` inline

Copy: `'Delete "{name}"? This cannot be undone.'`
Only available on draft sequences with no enrollments. Sequences with any enrollment record are archived, not deleted.

### Loading States

- List page: `<Spinner size="lg" />` centered in card, `py-16`
- Builder page: `<Spinner size="lg" />` centered `h-64` (edit mode only, while sequence loads)
- Results page: `<Spinner size="lg" />` centered `py-24`
- Save draft button: `loading` prop on Button (spinner inline)
- Start Sequence button: `loading` prop on Button

### Error States

- Builder validation: inline `text-sm text-red-400` beneath each invalid field, `role="alert"`
- Delay ordering violation: "Step delays must be strictly increasing (each step's day must be greater than the previous step's day)."
- Network error (toast): uses existing `showToast(message, 'error')` pattern
- Results page load error: `text-sm text-red-400` inline — "Failed to load sequence results. Try refreshing the page."

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — list page | "New Sequence" |
| Primary CTA — builder (start) | "Start Sequence" |
| Primary CTA — builder (save) | "Save draft" |
| Empty state heading — list | "No sequences yet" |
| Empty state body — list | "Create your first drip sequence to automatically follow up with your audience." |
| Empty state CTA — list | "New Sequence" (primary button, Plus icon) |
| Empty state — step panel | "Write your email content here..." (TipTap placeholder) |
| Results — step not sent | "Step not sent yet. Stats will appear after this step runs." |
| Results — no enrollments | "No contacts enrolled. Start the sequence to enroll your contact list." |
| Error — load failure | "Failed to load sequence results. Try refreshing the page." |
| Error — save failure | Toast: "Failed to save sequence. Please try again." |
| Error — start failure | Toast: "Failed to start sequence. {server message}" |
| Destructive — delete | window.confirm: "Delete \"{name}\"? This cannot be undone." |
| Destructive — archive | window.confirm: "Archive \"{name}\"? Active enrollments will stop receiving steps." |
| Destructive — pause | window.confirm: "Pause this sequence? Active contacts will stop receiving steps until you resume." |
| Start modal — heading | "Start this sequence?" |
| Start modal — body | "This will enroll {N} active contacts from {list name}. Emails will begin sending automatically. Contacts added after this point will not be enrolled." |
| Start modal — confirm | "Start Sequence" |
| Start modal — cancel | "Cancel" |
| Section label — builder settings | "Sequence Settings" |
| Section label — builder steps | "Steps" |
| Step header | "Step {N}" |
| Step delay label | "Send on day" + trailing "(days after enrollment)" |
| Add step button | "Add Step" |
| Pause button (results page) | "Pause Sequence" |
| Resume button (results page) | "Resume Sequence" |
| Enrollment summary | "{N} contacts enrolled · {Status}" |
| Step results heading | "Step {N} — Day {delay_days}" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party | none | not applicable |

No shadcn or third-party registries in use. All components are hand-authored with Tailwind CSS.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
